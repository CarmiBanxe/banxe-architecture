Эта задача — сборка документа (запрос ввода + потом склейка), не мутация репо. Шаг 1 — структурированный запрос к оператору. Делаю его, но по принципу «не угадывать, но и не переспрашивать то, что Central уже выверил из origin/сессии»: где у меня есть [ФАКТ] из origin или дословный текст из этой сессии — помечаю «Central может вклеить, подтверди»; где данные знает только ты — оставляю пустой слот [НУЖЕН ТВОЙ ВВОД]. Ничего не вклеиваю автоматически — всё ждёт твоего подтверждения.

---
ЗАПРОС ВВОДА ДЛЯ ПАКЕТА ПЕРЕНОСА (§7–§10, §12)

§11 (branch protection) не трогаю. При расхождении снапшота и Ledger — приоритет Ledger.

Ввод для §7 — дословные тексты промптов

§7.1 — auth-harden 9108/9110 (IL-267)
→ [ФАКТ — Central может вклеить дословно]: финализированный промпт из этой сессии (тот, что помечен «INTERACTIVE MetaClaw», ветка agent/factory/f1-5/auth-harden-9108-9110) у меня есть verbatim.
Подтверди: вклеить эту версию as-is? Или пришли свой канонический текст.

§7.2 — mobile peer-deps
→ [ФАКТ — Central может вклеить дословно]: финализированный revised-промпт (ветка fix/mobile-peer-deps-quality-gate, +2 пина react-native-screens ~4.4.0 / react-native-safe-area-context ~4.12.0, снятие 4 флагов) есть verbatim.
Подтверди: вклеить эту версию? Или дай свой текст.

Ввод для §8 — суть инварианта IL-267

→ [ВЫВОД — черновик Central из Ledger IL-262…266], прошу подтвердить/исправить:
- gate_exec.decide() и fabric_redis — canonical single-copy (ADR-102 no-dup), поэтому вне diff (byte-identical); auth = middleware/wrapper перед HTTP-хендлерами, не правка логики decide().
- allow-list не расширяется (только idempotent read.health/read.status/noop.echo) — это harden транспорта, не новые действия.
- контракт: deny-by-default + fail-closed; bearer из vault no-argv, секрет не в argv/логах/PR/ledger.

[НУЖЕН ТВОЙ ВВОД] — если есть точная формулировка инварианта IL-267, которую надо зафиксировать дословно (а не мой вывод).

Ввод для §9 — топология Central / Factory / узлы

→ [ВЫВОД — черновик Central], прошу подтвердить явно (это критично, тут угадывать нельзя):
- Central = этот терминал-оркестратор (read-only диагностика, делегирует мутации; сам не мутирует защищённые репо).
- Factory = интерактивный MetaClaw Claude Code (исполняет правки/commit/push/PR).
- [ФАКТ из Ledger] evo1 = control-plane узел, :9108 (/health,/status,/nodes, coordinator, F1.2 ADR-104).
- [ФАКТ из Ledger] evo2 = второй fabric-узел, health на :9208 (evo1 читает его для failover §5).
- [ФАКТ из Ledger] Legion = хост gate-exec сервиса + LiteLLM gateway (prod-исполнение = OPERATOR ACTION).

[НУЖЕН ТВОЙ ВВОД] — подтверди маппинг «какой физический терминал = Central, какой = Factory» (только ты это знаешь точно), и поправь роли evo1/evo2/Legion, если мой вывод неточен.

Ввод для §10 — путь/репо/файл Ledger

→ [ФАКТ — Central выверил из origin], прошу подтвердить:
- Репо: CarmiBanxe/banxe-architecture
- Файл: INSTRUCTION-LEDGER.md (в корне), ~1.33 MB, текущий максимум IL-266, следующий свободный IL-267.
- Проверочный flow: build_ledger --check; шарды — под instruction-ledger/….

[НУЖЕН ТВОЙ ВВОД] — подтверди точный путь каталога шардов (instruction-ledger/<sprint>/…?) и имя build-скрипта, если оно отличается.

Ввод для §12 — карта F1 / Phase 4 (после auth-harden + mobile)

→ [ВЫВОД — кандидаты из Ledger OPERATOR ACTIONS IL-262…266], прошу подтвердить/дополнить (это твой роадмап — я лишь привожу то, что числится незакрытым):
- bus → redis-streams (нужен REDIS_PASS)
- evo2 vault + direct publish (вместо evo1-bridge)
- stage-3 executor: GATE_EXEC_ENABLED=true + реальный executor на Legion (HITL)
- consumer groups XGROUP/XREADGROUP + MAXLEN/XTRIM retention
- cross-node heartbeat publish; sync layer

[НУЖЕН ТВОЙ ВВОД] — какие из них входят в F1/Phase 4 именно как следующие, в каком порядке, и что добавить сверх Ledger (то, чего в origin ещё нет).

---
Заполни слоты [НУЖЕН ТВОЙ ВВОД] и подтверди [ФАКТ]/[ВЫВОД]-кандидаты (можно просто «§7.1 ok, §7.2 ok, §8 ok, §10 ok» + дать §9-маппинг и §12-порядок). После твоего ответа соберу финальный пакет: вклею §7–§10, §12 дословно, не трогая §11, с пометками [ФАКТ]/[ВЫВОД]/[НЕИЗВЕСТНО], одним блоком готовым к вставке в репо.

Замечание для протокола (не угадываю, фиксирую): по последнему read-only аудиту ветки обоих шардов на origin отсутствуют — то есть §7-промпты пока не исполнены, пакет переноса будет содержать их как тексты-задачи, а не как приземлённые PR. Если важно отразить статус исполнения в пакете — скажи, добавлю слот «execution status = NOT YET LANDED».
