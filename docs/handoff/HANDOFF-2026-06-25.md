# BANXE EMI AI BANK — HANDOFF (current, replaces all prior snapshots)
binding 2026-06-25 23:00 CEST

## STATE (live-verified)
- banxe-architecture (docs-plane): main IL=540, ledger --check OK from root.
  ALL SPEC-LOCKED: P0 critical path (J/E/D-recon/K/F-finrpt) + 13 P1 + non-gated P2
  (B-pricing, F-fatca, C-swift, E-treasury, H-crm, H-support, M-gateway, M-sandbox).
  Remaining docs: L-bi, M-sdk (P3) — non-gated, lowest priority.
- banxe-emi-stack (RUNTIME): main = a940e61. Implemented this session (sandbox-autonomous):
  - D-gl: #214 fail-closed, #215 lifecycle, #216 api-503, #217 approval-audit.
  - D-recon/E-safeguard: #218 3-leg engine, #219 wire-3leg-agent, #220 cron, #221 fca-notify,
    #222 ch-streak, #223 breach-notify-port, #231 E-D acceptance, #213 safeguarding-engine.
  - K-gabriel: #224 governor, #228 api(HITL), #229 regdata-adapter, #230 breach-handler.
  - F-finrpt: #233 regdata-runtime.
  - Stubs (27 NotImplementedError resolved): #234-242 (BT-002/004/005/006/007/008/009/010/
    011/012/013/014/015 + backup/alertstore/sardine/FOS).
  - Errata: #243 (append-only forward-fix for IL DUP #218/#219).

## MODE
- SANDBOX-AUTONOMOUS. Claude Code defaultMode=bypassPermissions (no per-cmd questions).
  Factory chains cycles, auto-merges green+CLEAN PRs, reports each SHA+IL, no go-words.
- Config: deny-list has gh pr close/edit/review but NOT gh pr merge. Branches go stale after
  each merge → re-sync (temp-merge onto fresh main) before auto-merge. On guard/stub conflict
  ALWAYS take the guard-preserving side.

## LEDGER CANON (PER-REPO — critical, do not confuse)
- banxe-architecture: numeric IL-NNN in ledger/IL-SEQUENCE.json; build_ledger.py --check FROM ROOT.
- banxe-emi-stack: named IL key [IL-CBS-*-DATE] in COMMIT MESSAGE (NO build_ledger.py);
  verify via git log; each PR MUST carry a UNIQUE key (lesson from #218/#219 DUP →
  parent+dependent need distinct keys, add -WIRE/-API/-TESTS suffix).

## HARD INVARIANTS (never lifted, even sandbox)
- Required CI + guardians GREEN before merge (Semgrep×3 required — never bypass; nosemgrep only
  for justified non-money cases). Append-only (no renumber/rewrite; fix DUP via forward errata).
  No --admin/force-push. Offline/no-live/no-PII/Decimal-only. ADR-102 dedup + live-audit each cycle.
  STOP-on-doubt (fabrication/failing-gate/ambiguity).
- PRODUCTION GUARDS STAY: NotImplementedError/RuntimeError on live ops (LiveRegDataClient.submit,
  Fineract failover, live-submit) must NOT be removed; adapt TESTS to the guard, never guard to tests.

## OPERATOR-GATED (skip with 1-line brief, do not stop chain)
- unify safeguarding_events (cross-regime audit source-of-truth) — architectural, awaits operator.
- Fineract failover (no API ref in-repo; spec operator-gated) · live-infra activation ·
  CLASS_B passport activation · Arch-WG DRAFT promotion (banxe-news/banxe-portfolio repos = 404).

## PRODUCT-LEVEL HITL (runtime behavior per spec — kept, not pacing)
MLRO SAR filing · CFO treasury ≥£100k sign-off · CASS shortfall → MLRO/CFO · no autonomous
breach suppression · K-gabriel HITL approve/reject.

## NEXT (autonomous, by critical path)
- Remaining real runtime gaps (live-audit, non-dup) across D-recon/E-safeguard/K/F.
- L-bi / M-sdk docs-specs (P3, banxe-architecture) — lowest priority.
- Operator decisions pending: unify safeguarding_events; Fineract; passports; Arch-WG DRAFTs.

## FIRST ACTION IN NEW SESSION (read-only)
cd ~/banxe-emi-stack && git fetch origin main -q && git rev-parse --short origin/main && \
  git log --oneline -8 origin/main && gh pr list --state open --limit 10
cd ~/banxe/banxe-architecture && git fetch origin main -q && python3 ledger/build_ledger.py --check 2>&1|tail -1
# emi-stack IL uniqueness: git log origin/main --oneline -80 | grep -oE 'IL-CBS-[A-Z0-9-]+' | sort | uniq -d
#   (IL-CBS-DRECON-3LEG dup is HISTORICAL, resolved by errata #243 — expected, not a new breach)
