---
il_ts: 2026-07-01T19:00:00Z
session_id: agent-factory-governance-server-2-borrowable-compute
source: CEO
status: DONE
---
### [OWNER: A] Server-2 borrowable-compute orchestration — policy + config-as-data (factory operating-model addition)
- **Decision:** Per operator authorization of the server-2 package, authored TWO artifacts: `docs/governance/SERVER-2-BORROWABLE-COMPUTE-ORCHESTRATION.md` (policy + canon integration) and `config/compute/server-2-borrow-policy.yaml` (ALL thresholds as data, Config-over-Hardcoding CLAUDE.md §10, proposed defaults for operator ratification). Defines server-2 (evo2, 192.168.0.15, `qwen3:235b-a22b-banxe` 142 GB) as a **borrowable compute pool**: primary tenant = EMI BANXE project (standing 235b-slot reservation), secondary = factory burst/borrow (revocable lease on UNUSED capacity only). **PREPARE-ONLY**, Draft PR. Owner A.
- **Policy core:** strict priority `project_critical › project_batch › factory_heavy_analysis › factory_audit › experimental` — any project_* > any factory_*/experimental (hard invariant, allocator may NOT override). 235b = single serialized slot (142 GB, no co-residency); contention resolved by admission ordering, not fractional GPU. Borrow conditions (all): primary queue idle ≥ idle_grace_s + health-check pass + concurrency < ceiling + budget/duty not exhausted. Revocation: project_* arrival → stop admitting + graceful-drain-then-defer (drain_deadline_s); runaway > runaway_kill_after_s → hard-stop. Fallback = **FAIL-CLOSED** (no telemetry / health-fail / config unreadable ⇒ borrowing off, server-2 project-only, factory heavy → factory-* on evo1).
- **Placement via EXISTING alias prefixes:** project-* = primary lane, factory-* = borrow lane, both on the same 235b slot; NO new models/endpoints. 5 workload classes each mapped to placement + priority + preemptibility (config `workload_classes`).
- **Resource governance:** admission control (not GPU partitioning); project hard reserved floor; factory soft ceiling (default 1, auto-0 when any project_* active); idle_grace 90s; drain 120s; caps max_runtime 900s / daily 2M tokens / runaway 1200s; anti-monopoly max_duty_cycle 0.30 (factory) + 0.60 (project_batch). All numbers in config, none in doc/code.
- **Observability:** per-lane metrics (utilization/queue/failed/throughput/idle/reclaim/health); mandatory pre-borrow health-check (reachability/model-loaded/memory/error-rate → deny+log on fail); reuse existing LiteLLM prometheus callback (add only borrow counters, pointer); factory-journal events (grant/deny/revocation/preempt_defer/runaway_kill/budget_exhausted/health_fail) with correlation_id/tenant/class/alias/duration/tokens/reason.
- **Factory integration (additive to ADR-154):** factory = single allocator (class → lane/host/alias, admit/defer); central/right terminals submit class-tagged workloads, do NOT self-allocate; compute-ownership zones recorded ∥ write-zones in TERMINAL-OWNERSHIP; contention via CONFLICT-LEDGER; project-priority invariant structurally above allocator discretion; **ADR-102 dedup + quality-gate.sh byte-UNCHANGED** (borrow changes WHERE, not WHAT-must-pass).
- **Acceptance (testable):** docs+config exist, thresholds in config not code; class→placement/priority table complete (5); policy-conformance SIMULATION (dry-run job-trace, no hardware mutation) shows project_critical before pending borrow within drain_deadline, borrow denied on non-empty queue/health-fail, duty-cycle ≤ cap; journal has grant/deny/revocation/kill; fail-closed demonstrated; ADR-102+quality-gate byte-unchanged. **Runtime enforcer/scheduler = separate downstream slice (project/infra), NOT here.**
- **Risk notes:** starvation (project-priority invariant + drain + batch duty-cap); hidden contention (serialized single 142GB slot + idle_grace + short caps); runaway (runtime/token/duty caps); insufficient observability (borrow conditional on live telemetry → fail-closed); **false "100% utilization" target explicitly REJECTED** (goal = project SLO + zero project-preemption; utilization is secondary observed metric, not an optimization target).
- **Boundaries:** NO runtime-enforcer/scheduler; LiteLLM gateway configs NOT touched; hardware/machines NOT touched; banxe-ui NOT touched; uiux-pipeline.sh NOT touched. Only the 2 files + this shard. 0 off-scope.
- **Anti-dup (ADR-102) pointer-first:** references ADR-154 (factory-as-arbiter, extended to compute additively), TERMINAL-OWNERSHIP (compute zones ∥ write-zones), CONFLICT-LEDGER (contention), agents.md alias set (project-*/factory-*/reasoning-235b), existing LiteLLM prometheus callback — restates none; no parallel governance model, no config duplication.
- **[НЕИЗВЕСТНО] / operator-ratification (non-blocking):** exact project_critical SLO + all borrow thresholds = proposed defaults (config `#ratify`), not final; evo2-headless-claude host = separate infra ticket; runtime-enforcer = downstream slice.
- **Scope/flow:** authored per #900 — 2 files + paired shard ATOMIC; NO hand-edit of generated ledger; NO hardcoded IL (build_ledger mints, ADR-119 Rule 8). YAML validated (priority_order parses to the 5-class list; inline-comment fusion bug caught+fixed).
- **Proof:** IL provisional (ADR-119 Rule 8) — max+1 over origin/main (max 777) via allocator (ADR-143/143-A); unique, 0 dups; 1:1 (ADR-144). Append-only: ONE tail shard, il_ts `2026-07-01T19:00:00Z` > main max. Fresh worktree off origin/main `ee99132` (ADR-120/060). FROZEN/.canon untouched.
- **Status:** DONE — policy doc + config-as-data + shard. **DRAFT PR; DO NOT MERGE — operator HITL. Next: operator ratifies config defaults; then the runtime-enforcer slice (project/infra-side, downstream).**
- **Refs:** `docs/governance/SERVER-2-BORROWABLE-COMPUTE-ORCHESTRATION.md`; `config/compute/server-2-borrow-policy.yaml`; ADR-154; TERMINAL-OWNERSHIP; CONFLICT-LEDGER; agents.md; ADR-102/143/143-A/144; CLAUDE.md §10/§11; #900. Operator directive 2026-07-01 (server-2 package authorized).
