---
il_ts: 2026-06-25T18:00:00Z
session_id: agent-factory-factory-skills-bind-group1
source: CEO
status: DONE
---
### Skills-binding GROUP 1 — bind passport skills to 10 unbound L1/L2 line-of-defence/orchestration agents (S-FAC-64 style, PROPOSED/I-27, binding metadata only)
- **Decision:** Applied passport-bound-skills blocks (`allowed_skills` + `preferred_skill_sequences` + `mandatory_skill_triggers`, S-FAC-64 style) to the 10 GROUP-1 agents from `scripts/skills-bind-audit.sh` proposals. Bound ONLY proposed skills (⊆ SKILLS-MATRIX); NO activation — each passport's existing `status` unchanged (binding metadata, I-27). Minimal-diff: each block appended after the passport's last existing field; all existing fields/order preserved.
- **Per-agent skills assigned + justification (each re-verified against capabilities, not force-fit):**
  - **board_reporting_agent, ceo_orchestration_agent, cfo_orchestration_agent, coo_operations_agent, front_office_agent** (dept-head stubs, capabilities=department_head_orchestration) → `context_memory_sync` (M), `rapid_spec_builder` (M). Both genuinely apply: dept-heads that PROPOSE IL/ADR (I-27) and need IL/audit continuity (I-28).
  - **compliance_monitoring_agent, risk_oversight_agent** (2nd-line independent functions) → same 2 MANDATORY + `prohibited_skills: [auto_refactor_pro]` (independence — must not auto-refactor reviewed code, I-20).
  - **cto_platform_agent** → 2 MANDATORY + `dependency_optimizer` (C): technology/platform head genuinely owns Python/Docker deps + licence audit (IL required).
  - **legal_corporate_agent** → 2 MANDATORY + `prohibited_skills: [auto_refactor_pro]`. **DROPPED 4 proposed code skills** (clean_architecture_enforcer, error_handling_standardizer, api_contract_guardian, smart_test_generator) — justified: false-positive in skills-audit, department "Corporate **Services**" matched the `service` keyword; this is a STUB dept-head with NO ports/service code, so the code skills do not genuinely apply (documented inline in the passport).
  - **banxe_aml_orchestrator** (REAL Level-1 orchestrator v3.0.0 WITH ports PolicyPort/EmergencyPort/AuditPort/DecisionPort + AML scoring) → 8 skills: `context_memory_sync`, `rapid_spec_builder`, `clean_architecture_enforcer` (M, no cross-contour imports I-20), `error_handling_standardizer` (M, typed exceptions on ports, AuditPort survives I-24), `api_contract_guardian` (M, owns 4 port contracts), `performance_scanner` (M, AML score-aggregate time-critical), `smart_test_generator` (C, port tests; no PII/float I-05/no mocked audit I-24), `dependency_optimizer` (C, adapter deps) + `prohibited_skills: [auto_refactor_pro]` (AML compliance contour — ARP forbidden on services/aml/, I-20, SKILLS-MATRIX §9). All genuinely apply (real ports/services, unlike the stubs).
- **Basis:** `make skills-audit-json` proposals 2026-06-25 (all ⊆ SKILLS-MATRIX); SKILLS-MATRIX.md (10 skills, per-plane MANDATORY/CONTROLLED + Trigger column → `mandatory_skill_triggers`); S-FAC-64 binding precedent (internal_audit_agent / resilience_agent format); CANONICAL-ORG-CHART-v2 (line-of-defence → prohibited_skills for independent functions).
- **Proof — deltas:** `make skills-audit` (`--self-test`) bound **9 → 19** (+10), unbound **48 → 38** (−10); all 10 GROUP-1 agents confirmed unbound→bound (json check `all 10 bound = True`). `make train-verify` = **no regression**: still exits 2 solely on the pre-existing `cicd_quick_setup` gap (the only unbound mandatory skill before this PR; not a GROUP-1 proposal, correctly not force-fit) — every other mandatory skill bound, `clean_architecture_enforcer` coverage improved (bound_ids 8→9). All 10 passports parse (`yaml.safe_load` OK); skill counts 2/2/2/2/2/2/2/3/2/8 + prohibited on 4. semgrep pre-commit green.
- **Append-only (ADR-059-A):** ONE new tail shard at il_ts `2026-06-25T18:00:00Z` (strictly > fresh tree max `2026-06-25T17:30:00Z`); INSTRUCTION-LEDGER.md + IL-SEQUENCE.json regenerated via `python3 ledger/build_ledger.py`, `--check` exit 0. Isolated worktree off `origin/main 673bef8` (ADR-120); RULE 7 self-honored (only own worktree). Diff = exactly 10 passports + shard + INSTRUCTION-LEDGER.md + IL-SEQUENCE.json.
- **Status:** DONE — GROUP 1 (10 agents) bound; passports remain PROPOSED/non-activated (I-27). DO NOT MERGE pending operator review.
- **Recommended next (operator):** merge; then GROUP 2 binding sprint (next unbound L2/L3 agents — adapters, governors, integration agents via `make skills-audit`); separately bind `cicd_quick_setup` (the one unbound mandatory skill) to a CI/infra-owning passport to close `train-verify` (S-FAC-66).
- **Refs:** 10 passports under `agents/passports/`; `docs/SKILLS-MATRIX.md` (source of truth); `scripts/skills-bind-audit.sh` (proposals); `scripts/train.sh` (train-verify); S-FAC-64 (binding precedent); `governance/CANONICAL-ORG-CHART-v2.md` (line-of-defence); ADR-059-A, ADR-120, ADR-121, I-20/I-24/I-27/I-28.
