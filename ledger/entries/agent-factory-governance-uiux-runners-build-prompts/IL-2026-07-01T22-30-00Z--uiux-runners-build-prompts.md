---
il_ts: 2026-07-01T22:30:00Z
session_id: agent-factory-governance-uiux-runners-build-prompts
source: CEO
status: DONE
---
### [OWNER: A] UI/UX runners BUILD-PROMPTS — one consolidated doc, four project-side runner tasks
- **Decision:** Per operator "author the runner build-prompts", authored `docs/governance/UIUX-RUNNERS-BUILD-PROMPTS.md` — ONE consolidated governance document with four runner build-prompts for banxe-ui (deliberately one doc, not four, to avoid ledger-contention). These are TASKS for the project side, executed by the operator inside banxe-ui beyond the ADR-117 perimeter — NOT runner code. Derived from emission-spec #928 §4, gate-policy #918, ingest #921; anchored to banxe-ui b9645a2. **PREPARE-ONLY**, Draft PR. Owner A.
- **Shared frame (all four):** each runner runs project-side in banxe-ui CI, emits its own result into the SAME envelope `evidence/uiux-findings.json` (P0 schema, contract_version 1.0.0) consumed by ingest #921; **all four ADVISORY per gate-policy #918 — do NOT block, do NOT touch factory blocking/exit; axe-core/WCAG stays the sole hard runtime gate**; freshness/honesty (no evidence ⇒ unknown, never pass; commit_sha = frontend commit); result shape per P0 (requirement/status/severity:advisory + optional fields, severity read from policy §3 not invented); task-not-code; build on existing Storybook/axe-core/vitest, do NOT rebuild.
- **Four sections:** (1) Playwright e2e_journeys (canonical journey list = [UNKNOWN]); (2) visual_regression diffing over the EXISTING 13 Storybook stories — add only diffing (Chromatic/Loki/Percy/reg-cli = implementer choice), NOT duplicate Storybook; (3) viewport_matrix (breakpoint set = [UNKNOWN] from design-system); (4) state_coverage empty/loading/error per component (component inventory = [UNKNOWN] from banxe-ui). Each: what to build + what to emit (requirement name + P0 fields) + acceptance (schema-valid; ingest unknown→real status for that requirement; others correctly unknown) + [UNKNOWN] implementer details.
- **Anti-dup (ADR-102) pointer-first:** references emission-spec #928, emitter build-prompt #942, ingest #921, schema+policy #918, runtime-contract #920, ADR-117 — restates none. Explicitly NOT duplicating the emitter prompt (that = emitter/transport; this = the four runners generating evidence the emitter packages) NOR Storybook (exists — build on it). No parallel schema/policy, no code.
- **Boundaries:** NO runner code; banxe-ui repo NOT touched; P0 schema NOT touched; uiux-pipeline.sh ingest NOT touched; emitter prompt NOT touched; policy/spec NOT touched; Storybook NOT rebuilt. Only this doc + shard. 0 off-scope. All four advisory — factory blocking/exit byte-unchanged. Baseline of fact: banxe-ui origin/main b9645a2 (2026-06-27, two clones).
- **Scope/flow:** authored per #900 — doc + paired shard ATOMIC; NO hand-edit of generated ledger; NO hardcoded IL (build_ledger mints, ADR-119 Rule 8). Re-mint discipline if mid-session collision (wave active): reset onto origin/main + regenerate; recreate shard AFTER reset (lesson #933).
- **Proof:** IL provisional (ADR-119 Rule 8) — max+1 over origin/main (max 791) via allocator (ADR-143/143-A); unique, 0 dups; 1:1 (ADR-144). Append-only: ONE tail shard, il_ts `2026-07-01T22:30:00Z` > main max. Fresh worktree off origin/main (ADR-120/060). FROZEN/.canon untouched.
- **Status:** DONE — four-runner build-prompt set + shard. **DRAFT PR; DO NOT MERGE — operator HITL. Next: operator hands each runner section to the project side to build inside banxe-ui under the ADR-117 gate; each flips its requirement unknown→real evidence when built.**
- **Refs:** `docs/governance/UIUX-RUNNERS-BUILD-PROMPTS.md`; UIUX-EVIDENCE-EMISSION-SPEC.md (#928); BANXE-UI-EMITTER-BUILD-PROMPT.md (#942/IL-791); UIUX-RUNTIME-CONTRACT.md (#920); schema + UIUX-GATE-POLICY.md (#918); uiux-pipeline.sh ingest (#921); ADR-102/117; #900. Operator directive 2026-07-01 (author the four runner build-prompts, one consolidated doc).
