---
il_ts: 2026-06-18T15:00:00Z
session_id: agent-factory-m1-instrument-params-m1-9
source: CEO
status: DONE
---
### M1.9 config-as-data advisory instrument trading-parameters (code substep, additive/read-only) — BANXE.RAR → EMI
- **Instrukciya:** M1.9 code-подшаг: заменить hardcoded stub GET /instruments/{symbol} детерминированным config-as-data advisory source (per-symbol tick/min/max DecimalStr + fee_schedule_ref string), reuse SymbolInfo/MarketDataPort universe, InstrumentInfo frozen, без дублирования fee-логики (FeeEnginePort = fee SoT). Server-only evo1 в ИЗОЛИРОВАННОМ worktree (после cross-session leak инцидента; Rule 1 verify branch перед каждым commit); advisory/read-only/mock-safe, fail-closed; Decimal/I-01 (trading-params не balances). Append-only IL-shard (ADR-059-A); promotion-PR без merge (ADR-102).
- **Shagi:** banxe-trading-backend ветка agent/factory/m1/instrument-params-m1-9 от main b6dd0af (isolated worktree /tmp/wt-m1.9-instrument-params). Новый src/banxe_trading_backend/instruments/params.py: _INSTRUMENT_PARAMS config-as-data (BTC-EUR/ETH-EUR из symbol universe: tick_size/min_qty/max_qty DecimalStr + fee_schedule_ref spot-default) + instrument_info(symbol) с normalize + fail-closed InstrumentParamsError на unknown (no fake). api/symbols.py: get_instrument теперь читает instrument_info() (stub-тело + ADR-021 TODO-заглушка удалены; unknown → HTTPException 404; убран неиспользуемый split_symbol import; добавлен HTTPException; docstring обновлён). InstrumentInfo контракт frozen (те же поля). Fee-логика не дублируется (только fee_schedule_ref string, FeeEnginePort = fee SoT). Reuse SymbolInfo/MarketDataPort universe (no second instrument SoT). Тесты tests/test_instrument_params.py (+8): characterization (config не stub, universe coverage из MarketDataPort.list_symbols, normalization btc/eur→BTC-EUR, endpoint 200 feeScheduleRef=spot-default), contract (InstrumentInfo/SymbolInfo/EarnMetrics frozen + /symbols + /earn/rates + /accounts/metadata + /assets/metadata 200), negative fail-closed (unknown → InstrumentParamsError, endpoint 404). Валидация (main venv via PYTHONPATH): ruff clean, mypy strict clean (79 files), pytest 401 passed (393+8); smoke GET /instruments/BTC-EUR 200 (config), DOGE-EUR 404. Promotion-PR banxe-trading-backend #42 OPEN (no merge). IL-shard в banxe-architecture isolated worktree.
- **Proof:** Duplication Audit (ADR-102): InstrumentInfo + symbols seam (SymbolInfo/MarketDataPort; real upstream ADR-021 TODO) = canonical instrument SoT — reused/extended, NOT duplicated, no second instrument model; FeeEnginePort = canonical fee SoT, NOT duplicated (только fee_schedule_ref string); instrument params source не существовал (был stub) → создан ОДИН instruments/params.py; earn/accounts/assets keep untouched. Verdict: zero duplicates, single params source, keep/extend, no merge/delete (удалён только dead stub-body). build_ledger --check OK. Numeric (I-01): tick/min/max DecimalStr trading-params (granularity/order-size limits), НЕ balances/amounts/positions, no BigNumber/float; fee_schedule_ref string без вычислений. Activation Safety: read-only/advisory/mock-safe config-as-data; no live orders/execution/balances/positions/payments/fund movement; no live catalogue/exchange calls/network/keys; fail-closed unknown→404 (no fake). Out-of-scope: P0/regulated/live (orders/execution/balances/positions/payments), fee computation/billing, KYC/AML, cards, crypto-wallet ops, изменение SymbolInfo/InstrumentInfo/MarketDataPort/FeeEnginePort, второй instrument SoT, user/DB migrations, live state machines. Server-only в isolated worktree (no shared-clone contention, Rule 1/6 после инцидента); fabric/prod/secrets/emi-stack/governance-ADR не тронуты; branch protection не обойдён; merge не делал.
- **Refs:** banxe-trading-backend PR #42 (agent/factory/m1/instrument-params-m1-9); src/banxe_trading_backend/instruments/params.py, instruments/__init__.py, api/symbols.py (get_instrument config-driven, 404), tests/test_instrument_params.py; reuse models.py InstrumentInfo/SymbolInfo + ports/market_data_port.py MarketDataPort + ports/fee_engine_port.py FeeEnginePort (frozen SoTs, ADR-021); ADR-102, ADR-103, ADR-059-A; I-01; docs/migration/M1.9-instrument-advisory-params.md (plan, PR #519).
