---
il_ts: 2026-06-18T15:15:00Z
session_id: agent-factory-m1-instruments-list-m1-10
source: CEO
status: DONE
---
### M1.10 advisory instruments list/catalogue endpoint (code substep, additive/read-only) — BANXE.RAR → EMI
- **Instrukciya:** M1.10 code-подшаг: добавить read-only GET /api/v1/instruments list, выводящий уже-перенесённый M1.9 каталог (_INSTRUMENT_PARAMS) как list[InstrumentInfo]; reuse M1.9 single source + frozen InstrumentInfo, без нового DTO и без второго каталога; FeeEnginePort не дублировать (только fee_schedule_ref). Server-only evo1 в ИЗОЛИРОВАННОМ worktree (Rule 1 verify branch перед каждым commit); advisory/read-only/mock-safe, fail-closed; Decimal/I-01 (trading-params не balances/fees). Append-only IL-shard (ADR-059-A); promotion-PR без merge (ADR-102).
- **Shagi:** banxe-trading-backend ветка agent/factory/m1/instruments-list-m1-10 от main b1763a9 (isolated worktree /tmp/wt-m110c). instruments/params.py: добавлена list_instruments() — reuse _INSTRUMENT_PARAMS + instrument_info, детерминированный sorted list[InstrumentInfo], fail-closed (skip malformed config entry, no fabricated). api/symbols.py: новый GET /instruments → list[InstrumentInfo] (list_all_instruments); существующие GET /symbols + GET /instruments/{symbol} не тронуты. docs/specs/instruments-api.yaml: новый additive OpenAPI spec (InstrumentInfo schema не менялась). Нового DTO/второго каталога нет; _INSTRUMENT_PARAMS остаётся single SoT. Тесты tests/test_instruments_list.py (+5): characterization (deterministic list == sorted _INSTRUMENT_PARAMS keys, endpoint 200, no balance field), contract (InstrumentInfo/SymbolInfo frozen + MarketDataPort.list_symbols/FeeEnginePort.compute present + /symbols + /instruments/{symbol} unchanged), negative fail-closed (monkeypatch malformed entry → skipped). Валидация (main venv via PYTHONPATH): ruff clean, mypy strict clean (79 files), pytest 406 passed (401+5); smoke GET /instruments 200 [BTC-EUR,ETH-EUR], /instruments/BTC-EUR 200, /symbols 200. Promotion-PR banxe-trading-backend #43 OPEN (no merge). IL-shard в banxe-architecture isolated worktree.
- **Proof:** Duplication Audit (ADR-102): _INSTRUMENT_PARAMS (M1.9) + instrument_info = single instrument SoT — reused (list iterates существующий config), no second catalogue/params table; InstrumentInfo DTO reused frozen (no new model); FeeEnginePort = fee SoT, NOT duplicated (только fee_schedule_ref string, no fee math); SymbolInfo/MarketDataPort + earn/accounts/assets keep untouched. Verdict: zero duplicates, single SoT, keep/extend, no merge/delete. build_ledger --check OK. Numeric (I-01): tick/min/max DecimalStr trading-params (НЕ balances/amounts/positions/computed fees), no BigNumber/float. Activation Safety: read-only/advisory/mock-safe; no orders/execution/balances/positions/payments/fee computation/fund movement; no network/keys; fail-closed skip malformed (no fabricated instrument). Out-of-scope: P0/regulated/live (orders/execution/balances/positions/payments), fee computation/billing/preview, KYC/AML, cards, crypto-wallet ops, новый instrument DTO/второй каталог, user/DB migrations, live state machines, contract-breaking. Server-only в isolated worktree (Rule 1/6); fabric/prod/secrets/emi-stack/governance-ADR не тронуты; чужие ветки/untracked не тронуты; branch protection не обойдён; merge не делал.
- **Refs:** banxe-trading-backend PR #43 (agent/factory/m1/instruments-list-m1-10); src/banxe_trading_backend/instruments/params.py (list_instruments), api/symbols.py (GET /instruments), docs/specs/instruments-api.yaml, tests/test_instruments_list.py; reuse _INSTRUMENT_PARAMS/instrument_info (M1.9 IL-274), models.py InstrumentInfo/SymbolInfo, ports/market_data_port.py MarketDataPort, ports/fee_engine_port.py FeeEnginePort (frozen SoTs, ADR-021); ADR-102, ADR-103, ADR-059-A; I-01; docs/migration/M1.10-instruments-advisory-list.md (plan, PR #522).
