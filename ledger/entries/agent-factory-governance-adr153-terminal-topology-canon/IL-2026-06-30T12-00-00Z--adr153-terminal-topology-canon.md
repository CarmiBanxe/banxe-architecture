---
il_ts: 2026-06-30T12:00:00Z
session_id: agent-factory-governance-adr153-terminal-topology-canon
source: CEO
status: PREPARED
---
### ADR-153 (PROPOSED) — Terminal topology canon: A=left=Factory / Central / B=right=TRADING-001 [docs-only, reconcile]
- **Decision:** Created `docs/adr/ADR-153-terminal-topology-canon.md` — the single authoritative SoT for the terminal entities, per operator-declared truth: **Terminal A = LEFT = the Software Factory/orchestrator**, **Central** = production-line dispatcher/arbiter + operator-facing governance line, **Terminal B = RIGHT = special-mandate executor (current mandate = TRADING-001)**. **Sub-terminal A/B** reconciled as **bounded-context isolation UNITS** (worktree+branch; read+own-worktree+local-commit only; push/PR via orchestrator; merge operator-reserved) — NOT a fourth terminal.
- **ADR-102 dup-check / reconcile (not duplicate):** the audit found THREE incompatible legacy definitions — M1 AGENTS.md/`.claude/rules` ("Right=orchestrator/Left=agents", Best-Single-Artifact), M2 IL-7840-7842 (Central=Perplexity; A=left=sandbox; B=right=parallel-executor, BINDING), M3 global `~/.claude/CLAUDE.md` (Central=Right; "A builds the factory") — plus IL-7322/7323 (closed) and the Sub-terminal naming layer. ADR-153 maps each → canon (supersedes the IL/PROMPT-CANON/PLAYBOOK/ROADMAP topology statements; **reconciles** AGENTS.md behavioural canon by pointer, does NOT redefine it).
- **Key disambiguation (the conflict I flagged, now resolved by mapping):** "Right" was overloaded across a **behavioural** axis ("Right Terminal" = orchestration output-discipline) and a **topological** axis ("Terminal B (right)"). ADR-153 fixes the topological axis; the behavioural "Right Terminal" name is reconciled by an **additive naming pointer** added to `AGENTS.md` + `.claude/rules/agents.md` (rules unchanged — adds-to-never-overrides). A fuller rename to "Orchestrating Terminal" + global-CLAUDE.md alignment are flagged **operator-gated follow-ups** (NOT in this PR).
- **No silent override:** the AGENTS.md/`.claude/rules` Best-Single-Artifact behavioural RULES are untouched (only a one-line ADR-153 pointer added, operator-reviewed via `/.claude/ @mmber` CODEOWNERS at merge). Global `~/.claude/CLAUDE.md` (local, not git-tracked) is NOT edited — operator aligns out-of-band. No foreign-branch edits (Rule 6/7). Mechanism (Best-Single-Artifact, §71, Rules 1–7, ADR-120/121/060) unchanged.
- **Proof:** docs/canon-only (ADR-153 + 2 additive pointers + ledger); **no code / runtime / values / new repo / keys / secrets / RAR**; 0 files deleted, no ledger-history rewrite (legacy IL lines reconciled by reference, I-28). IL **provisional, NOT hardcoded** (ADR-119 Rule 8) — `build_ledger.py` mints max+1 over current `origin/main` (real frozen max 743 from `IL-SEQUENCE.json`; the `IL-2028` grep result is the frozen-typo phantom, ignored) → IL-744 via the ADR-143 allocator; on concurrent advance → rebase+regenerate (Rule 2/5), `--force-with-lease` only. Append-only (ADR-059-A): ONE tail shard, il_ts `2026-06-30T12:00:00Z` strictly > origin/main max. Branch off origin/main `aa5cc4e` (ADR-120; namespace ADR-060).
- **Status:** PREPARED — PROPOSED ADR-153 + additive pointers + shard. **DRAFT PR; DO NOT MERGE — governance decision, operator merge = enactment (CLAUDE.md §1/§9).** Source = CEO (operator-declared topology; factory prepared materials per §9).
- **Refs:** `docs/adr/ADR-153-terminal-topology-canon.md`; AGENTS.md + `.claude/rules/agents.md` (Best-Single-Artifact, additive pointer); IL-7840-7842 / IL-7322-7323 (superseded/closed); `PROMPT-CANON-PROJECT.md`, `OPERATOR-PLAYBOOK.md`, `ROADMAP.md`, ADR-044 (Sub-terminal, reconciled); ADR-120/121/060, parallel-session Rules 1–7, §71; BANXE-TRADING-001; ADR-102/119/143/059-A. Operator HITL.
