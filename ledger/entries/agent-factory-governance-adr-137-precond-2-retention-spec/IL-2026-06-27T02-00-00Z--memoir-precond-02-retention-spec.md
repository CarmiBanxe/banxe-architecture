---
il_ts: 2026-06-27T02:00:00Z
session_id: agent-factory-governance-adr-137-precond-2-retention-spec
source: CEO
status: DONE
---
### MEMOIR-PILOT-PRECOND-02 — Bounded retention + purge (config-as-data) — verifiable governance contract for ADR-137 Precondition #2
- **Decision:** Created `docs/governance/MEMOIR-PILOT-PRECOND-02-bounded-retention-purge.md` (PROPOSED, concept_only) — a **verifiable CONTRACT, not an implementation and NOT a real config file** — specifying ADR-137 Pilot Entry Precondition #2. **Scope:** Memoir factory-only pilot under the ADR-137/ADR-136 envelope. **Config-as-data:** retention/purge params are DATA, operator-owned, never hardcoded (CLAUDE.md §10); document specifies required field SCHEMA as contract only (`max_age`, `max_entries`, `purge_schedule`, `scope=fork+perimeter`) — **no real config file created**; ≥1 of max_age/max_entries must bind. **No indefinite accumulation:** finite upper bound by time AND/OR volume mandatory; absent/unparseable/unbounded/invalid config ⇒ **fail-closed (no pilot start / no writes)**, deny-by-default, no default-unbounded fallback. **Purge correctness:** irreversible (not recoverable via rollback/blame/checkout/merge), consistent with PRECOND-01 redaction (purged ≠ resurrectable — ADR-137 L79-80), bound-triggered. **Scope:** retention/purge strictly per fork+perimeter (ADR-117), no cross-perimeter storage. **Verification (held-out, passes ADR-135 gate, zero regressions):** no hardcoded values (config drives behaviour); bounded (over-limit → purge); fail-closed on bad config (fault-injection: no unbounded path); purge irreversibility; scope isolation. **Sign-off:** operator + IronClaw mandatory before #2 = DONE; else #2 open, no Memoir runtime. **Non-goal:** spec only — no engine/config-file/stub/deployment; implementation = subsequent gated work item.
- **ADR-102:** no prior retention/purge spec on main (docs/governance grep = PRECOND-01 sibling only); references ADR-137/136/135/117/130/127 + PRECOND-01 — bodies **NOT modified** (link-only). No duplication.
- **Proof:** docs/governance-only; concept_only; **no runtime, no code, no real config-file/stub, no secret, no import** of `github.com/zhangfengcdt/memoir`. ADR-136/ADR-137/agentmemory/CMS bodies untouched (only referenced). Append-only (ADR-059-A): ONE tail shard, il_ts `2026-06-27T02:00:00Z` strictly > origin/main max `2026-06-27T01:30:00Z`. IL **provisional, NOT hardcoded** (ADR-119 Rule 8) — `build_ledger` mints max+1 over current `origin/main` (max 563) → IL-564; frozen at rebase-before-merge. Validated by live ADR-133 uniqueness gate (540 allowlisted). Isolated worktree off origin/main `517ad46` (ADR-120); namespace ADR-060; no git ops outside this branch.
- **Status:** DONE — Precondition #2 spec PROPOSED (governance/concept-only). Draft squash PR; DO NOT MERGE — STOP before merge, operator HITL.
- **Refs:** `docs/governance/MEMOIR-PILOT-PRECOND-02-bounded-retention-purge.md` (NEW); ADR-137 (Precondition #2), MEMOIR-PILOT-PRECOND-01 (sibling), ADR-136 (envelope), ADR-135 (held-out gate), ADR-117 (perimeter), ADR-130/127, ADR-133 (uniqueness gate), ADR-102, CLAUDE.md §10; `github.com/zhangfengcdt/memoir` (external ref, NOT imported). Draft PR — operator HITL.
