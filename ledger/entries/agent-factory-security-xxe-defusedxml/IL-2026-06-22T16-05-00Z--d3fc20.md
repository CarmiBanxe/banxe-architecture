---
il_ts: 2026-06-22T16:05:00Z
session_id: agent-factory-security-xxe-defusedxml
source: CEO
status: DONE
---
### Security fix — close XXE in scripts/import_archimate.py via defusedxml (semgrep S320)
- **Instrukciya:** Закрыть pre-existing XXE finding (semgrep python.lang.security.use-defused-xml-parse / S320) на scripts/import_archimate.py:114,117, который блокировал ВСЕ локальные commit-ы repo-wide через .githooks/pre-commit (semgrep). НЕ использовать --no-verify, НЕ ослаблять hook — починить код. Ledger-coupled (ADR-056). Isolated worktree (Rule 1); local commit only — NO push, NO PR, NO merge.
- **Root-cause:** _etree_parse() использовал native xml parse (xml.etree / lxml без defuse) → XXE-уязвимость; suppression `# noqa: S320` маскировал finding на строке 114, но semgrep config=auto (ERROR) всё равно flag-ил оба call (114+117), блокируя pre-commit hook для всего репо.
- **Fix (minimal, behavior-preserving):** import-блок теперь биндит XXE-safe parser: try `from defusedxml.lxml import parse as _defused_parse` (lxml backend) / except ImportError `from defusedxml.ElementTree import parse as _defused_parse` (stdlib backend). _etree_parse() вызывает _defused_parse(str(xml_path)) → tree.getroot() (идентичный API/return type Any). Удалён неиспользуемый `_etree` import (избежать F401) и suppression `# noqa: S320`. Backend-selection через _USING_LXML сохранён. defusedxml 0.7.1 уже установлен в окружении; root dependency-манифеста (requirements*.txt/pyproject.toml/setup.*) в репо НЕТ → новый манифест не фабриковался (detect-don't-assume); defusedxml зафиксирован как требуемая зависимость в этом IL для будущего манифеста.
- **Proof:** semgrep --config p/python = 0 findings (было 2: lines 114,117); semgrep scan --config=auto --error --severity=ERROR = 0 findings / 0 blocking (зеркалит .githooks/pre-commit → hook теперь ПРОХОДИТ repo-wide). ruff check = All checks passed (нет F401 от удалённого _etree, нет F841). ast.parse OK. Functional smoke: реальный archimate/banxe-model.xml парсится через defused backend (root tag = archimate 3.0 model) — behavior-preserving. Изменён 1 code-файл (scripts/import_archimate.py) + ledger-coupling. Isolated worktree (Rule 1); parallel-session ветки не тронуты (Rule 6); local commit only — NO push/PR/merge.
- **Status:** XXE (S320) закрыт; pre-commit semgrep hook больше не блокирует commit-ы repo-wide. Фабрика разблокирована. defusedxml — обязательная зависимость (env: 0.7.1).
- **Recommended next:** operator review diff; затем (отдельно, по слову оператора) push + PR + CI-gated squash-merge на agent/factory/security/xxe-defusedxml-import-archimate. Рекомендация (follow-up, не в этом PR): добавить defusedxml в формальный dependency-манифест, когда таковой появится. Parallel-session ветки не трогать.
- **Refs:** scripts/import_archimate.py (_etree_parse + import block); semgrep python.lang.security.use-defused-xml-parse (S320); defusedxml 0.7.1 (defusedxml.lxml / defusedxml.ElementTree); .githooks/pre-commit (semgrep config=auto ERROR gate); .claude/rules/safety-rules.md (security canon); ADR-056 (ledger-coupling); I-28.
