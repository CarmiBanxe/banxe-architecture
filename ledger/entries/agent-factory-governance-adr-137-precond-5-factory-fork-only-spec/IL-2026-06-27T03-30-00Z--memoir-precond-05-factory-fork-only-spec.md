---
il_ts: 2026-06-27T03:30:00Z
session_id: agent-factory-governance-adr-137-precond-5-factory-fork-only-spec
source: CEO
status: DONE
---
### MEMOIR-PILOT-PRECOND-05 — Factory-fork-only / Project-fork disabled by default — verifiable governance contract for ADR-137 Precondition #5
- **Decision:** Created `docs/governance/MEMOIR-PILOT-PRECOND-05-factory-fork-only.md` (PROPOSED, concept_only) — a **verifiable CONTRACT, not an implementation** — specifying ADR-137 Pilot Entry Precondition #5. **Scope:** Memoir factory-only pilot under the ADR-137/ADR-136 envelope. **Factory-fork-first (ADR-137 L65):** pilot executes ONLY on the factory node / factory fork; no execution outside it (denied fail-closed). **Project-fork default-off (L105):** project fork DISABLED by default; enabling requires EXPLICIT operator approval — never automatic, never config-only. **No project deploy without approval:** any project deployment without explicit approval ⇒ fail-closed (deny-by-default, no writes/no pilot); "config says enabled" ≠ approval. **Non-prod project (L87):** even with approval — only non-sensitive/non-prod; RED zone (payment/KYC/AML/sanctions/ledger/secrets/PII) EXCLUDED (AI-FORBIDDEN, needs separate ADR+operator+IronClaw); production project FORBIDDEN within pilot. **Perimeter (ADR-117):** factory/project distinct perimeters; pilot doesn't cross perimeter without approval (composes with PRECOND-03). **Composition #1..#4:** factory-fork-only ⇒ redaction/retention/replay/one-substrate boundaries all apply within the factory fork. **Verification (held-out, passes ADR-135, zero regressions):** execution outside factory fork→blocked; project fork off by default (config-only enable rejected); project deploy without approval→fail-closed; RED zone/production project→forbidden. **Sign-off:** operator + IronClaw before #5 = DONE; else #5 open, no Memoir runtime. **Non-goal:** spec only — no deployment selector/config/stub/runtime; implementation = subsequent gated work item.
- **ADR-102:** no prior factory-fork-only spec on main (docs/governance grep = PRECOND-01..04 siblings only); references ADR-137/136/117/135 + PRECOND-01..04 — bodies **NOT modified** (link-only). No duplication.
- **Proof:** docs/governance-only; concept_only; **no runtime, no code, no config-file/stub, no secret, no import** of `github.com/zhangfengcdt/memoir`. ADR-136/137/117/agentmemory/CMS/PRECOND-01..04 bodies untouched (only referenced). Append-only (ADR-059-A): ONE tail shard, il_ts `2026-06-27T03:30:00Z` strictly > origin/main max `2026-06-27T03:00:00Z`. IL **provisional, NOT hardcoded** (ADR-119 Rule 8) — `build_ledger` mints max+1 over current `origin/main` (max 566) → IL-567; frozen at rebase-before-merge. Validated by live ADR-133 uniqueness gate (540 allowlisted). Isolated worktree off origin/main `cffb400` (ADR-120); namespace ADR-060; no git ops outside this branch.
- **Status:** DONE — Precondition #5 spec PROPOSED (governance/concept-only). Draft squash PR; DO NOT MERGE — STOP before merge, operator HITL.
- **Refs:** `docs/governance/MEMOIR-PILOT-PRECOND-05-factory-fork-only.md` (NEW); ADR-137 (Precondition #5; L65, L87, L105), MEMOIR-PILOT-PRECOND-01/02/03/04 (siblings), ADR-136 (fork split), ADR-117 (perimeter), ADR-135 (held-out gate), ADR-133 (uniqueness gate), ADR-102; `github.com/zhangfengcdt/memoir` (external ref, NOT imported). Draft PR — operator HITL.
