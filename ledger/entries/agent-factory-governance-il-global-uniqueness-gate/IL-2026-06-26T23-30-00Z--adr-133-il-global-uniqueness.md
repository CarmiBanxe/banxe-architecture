---
il_ts: 2026-06-26T23:30:00Z
session_id: agent-factory-governance-il-global-uniqueness-gate
source: CEO
status: DONE
---
### ADR-133 — IL global-uniqueness gate + PR-aware allocation (CI/deterministic layer; runtime-lease out of scope)
- **Decision:** Created `docs/adr/ADR-133-il-global-uniqueness-and-pr-aware-allocation.md` (ACCEPTED, concept_only) + patched `ledger/build_ledger.py` + `scripts/bx-session.sh`. Closes the atomic-IL-allocation ROOT (IL-540×2 historical, IL-550×2 in-flight) at the CI/deterministic layer **without any runtime dependency**. Root cause: `build_ledger.shard_key` enforces shard identity only by filesystem path (line ~77), never by IL value → duplicate VALUES slip past `--check`. (a) Added `check_global_uniqueness()` to `build_ledger.py` — wired into both `--check` and the write path; any IL value assigned to >1 shard FAILs exit 1; `ALLOWED_DUP_VALUES = {540}` allowlists the historical IL-540 dup (renumber FORBIDDEN, ADR-119) — NEW dups rejected. (b) `bx-session.sh il_advisory`: read-only PR-aware candidate IL = max(IL-SEQUENCE values, max IL-NNN in OPEN PR titles)+1, printed at session start with a Rule-8 caution (no write, mint unchanged). (c) Runtime-lease (Redis/etcd/Temporal) explicitly stays in `banxe-ai-infrastructure` per **ADR-060 §6 OUT OF SCOPE** — NOT added here. Mint logic (`assign`, max+1) unchanged.
- **Required gate, no workflow churn:** the uniqueness check lives inside `build_ledger.py --check`, already run by `.github/workflows/ledger-build.yml` and the `guardian-ledger` job (`guardian.yml`) → automatically REQUIRED, no new job / no YAML edit; the 540 allowlist prevents a false fail. **Tested:** normal `--check` exit 0 (540 allowlisted); remove 540 from allowlist → FAIL (540 caught); synthetic new dup → FAIL; re-allowlist → PASS.
- **ADR-102 dedup:** no prior IL-uniqueness/PR-aware ADR; `check_global_uniqueness`/`ALLOWED_DUP_VALUES`/`il_advisory` did not exist; #799 only RECORDS the debt, this IMPLEMENTS (a)/(b)/(c) (cross-linked). ADR # = **133** (130/131/132 in-flight, confirmed free on main). **NOT touched:** PRs #795/#796/#798/#799 + their files; mint logic; the 19 souls; IL-540 (allowlist only, no renumber). Append-only (ADR-059-A): ONE tail shard, il_ts `2026-06-26T23:30:00Z` strictly > last in-flight `2026-06-26T23:00:00Z` (#799). IL **NOT hardcoded** — `build_ledger` mints max+1 over current main (max 555 after #798/#796/#795/#799 merged) → IL-556 at rebase-before-merge (Rule 8). Isolated worktree off origin/main `f0dfe7a` (ADR-120); namespace ADR-060; concept-only; no runtime/secrets; no git ops outside this branch.
- **Refs:** `docs/adr/ADR-133-il-global-uniqueness-and-pr-aware-allocation.md` (NEW); `ledger/build_ledger.py` (PATCH — `check_global_uniqueness`, `ALLOWED_DUP_VALUES`); `scripts/bx-session.sh` (PATCH — `il_advisory`); `.github/workflows/ledger-build.yml` + `guardian.yml` (already run `--check`); ADR-119, ADR-060 (§3/§6), ADR-125 (IL-540 precedent), ADR-102, PR #799 (debt). Draft PR — operator HITL; DO NOT MERGE.
