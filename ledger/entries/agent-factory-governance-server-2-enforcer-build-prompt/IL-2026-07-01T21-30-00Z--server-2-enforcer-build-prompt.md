---
il_ts: 2026-07-01T21:30:00Z
session_id: agent-factory-governance-server-2-enforcer-build-prompt
source: CEO
status: DONE
---
### [OWNER: A] Server-2 enforcer BUILD-PROMPT — transferable task for project/infra-side construction
- **Decision:** Per operator "пиши команды — оператор исполняет", authored `docs/governance/SERVER-2-ENFORCER-BUILD-PROMPT.md` — the canonical ready-to-hand build-prompt for constructing the server-2 runtime enforcer, derived from the enforcer spec (#934/IL-782). This is a **TASK for the project/infra side, executed by the operator beyond the ADR-117 perimeter** — NOT enforcer code. **PREPARE-ONLY**, Draft PR. Owner A.
- **What to build (per prompt):** admission-controller reading the ratified `config/compute/server-2-borrow-policy.yaml` (all thresholds as data, incl. project_critical SLO=120) and executing admit/deny/drain/kill against the LiteLLM gateway (:4000) per policy #932. Ratified numbers surfaced for orientation (ceiling=1, idle_grace=90, drain_deadline=120, max_runtime=900, daily=2M, runaway_kill=1200, factory duty=0.30, batch duty=0.60, SLO=120) — READ from YAML, NOT hardcoded.
- **Invariants (from #934):** project-priority hard invariant; fail-closed; graceful-drain-then-defer (drain_deadline_s=120); runaway hard-kill (runaway_kill_after_s=1200); health-check-before-borrow; factory-journal events (grant/deny/revocation/preempt_defer/runaway_kill/budget_exhausted/health_fail + correlation_id/tenant/class/alias/duration/tokens/reason); reuse existing LiteLLM prometheus callback.
- **Config-driven (hard):** enforcer READS YAML, hardcodes nothing; threshold change = config edit + re-ratification, not code. Tenant/lane from alias prefix (project-*/factory-*); no new models/endpoints.
- **Acceptance (dry-run job-trace simulation, no hardware mutation):** project_critical before pending borrow within drain_deadline; borrow denied on non-empty queue/health-fail; duty-cycle ≤ max_duty_cycle + budget_exhausted; fail-closed on degraded telemetry/unreadable config; journal completeness; flip a config value → behaviour changes with NO code edit (decisive no-hardcode proof).
- **[НЕИЗВЕСТНО]:** exact gateway-integration mechanism (pre-call hook / sidecar / router / cancellation) = implementer's choice per deployed LiteLLM version; enforcer deployment point = operator/infra decision. Not invented.
- **Boundaries:** NO enforcer/scheduler code; LiteLLM gateway config NOT touched; gateway/machines NOT touched; banxe-ui NOT touched; server-2-borrow-policy.yaml NOT touched; policy doc NOT touched; enforcer spec (#934) NOT touched. Only this build-prompt + shard. 0 off-scope.
- **Anti-dup (ADR-102) pointer-first:** references #934 (spec — authoritative contract), #932 (policy), #933/#936 (ratified config), ADR-154, ADR-117, existing prometheus callback — restates none; no parallel spec/policy/config, no code.
- **Scope/flow:** authored per #900 — doc + paired shard ATOMIC; NO hand-edit of generated ledger; NO hardcoded IL (build_ledger mints, ADR-119 Rule 8). Re-mint discipline if mid-session collision: reset onto origin/main + regenerate; recreate shard AFTER reset (lesson #933).
- **Proof:** IL provisional (ADR-119 Rule 8) — max+1 over origin/main (max 785) via allocator (ADR-143/143-A); unique, 0 dups; 1:1 (ADR-144). Append-only: ONE tail shard, il_ts `2026-07-01T21:30:00Z` > main max. Fresh worktree off origin/main (ADR-120/060). FROZEN/.canon untouched.
- **Status:** DONE — build-prompt + shard. **DRAFT PR; DO NOT MERGE — operator HITL. Next: operator hands this build-prompt to the project/infra side to construct the enforcer under the ADR-117 gate.**
- **Refs:** `docs/governance/SERVER-2-ENFORCER-BUILD-PROMPT.md`; SERVER-2-RUNTIME-ENFORCER-SPEC.md (#934/IL-782); SERVER-2-BORROWABLE-COMPUTE-ORCHESTRATION.md (#932/IL-778); server-2-borrow-policy.yaml (#933/IL-780 + #936/IL-784); ADR-102/117/154; CLAUDE.md §10/§11; #900. Operator directive 2026-07-01 (fix transferable enforcer build-prompt).
