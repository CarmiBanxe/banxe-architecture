---
il_ts: 2026-07-03T11:00:00Z
session_id: agent-factory-consolidation-engine-fleet-sprint-b
source: CEO
status: DONE
---
### [OWNER: A] Engine-fleet Sprint-B package — install/access/health scripts + EngineHealthAgent passport (prepare-only); 0 installs, 0 daemons
- **Decision:** Per operator (Sprint-B real task: prepare install commands + simplified access + monitoring AI-agent), authored 5 files + this shard. Prepare-only. NOTHING installed; NO daemon started; NO source invented (blocked=blocked per #1004); NO auth bypassed; NO legal/ss1/GUYON/ADR/perimeter-config touched; NO passport outside the one created. **PREPARE-ONLY**, Draft PR. Owner A.
- **Files:** `scripts/engines/install-engines.sh` (idempotent, OPERATOR-RUN-ONLY, provenance-bound #1004); `scripts/engines/engines-access.sh` (~/bin/engines symlinks + engines-status); `scripts/engines/engine-health-check.sh` (read-only bin+port probe, ALERT on fail, no daemon); `config/agents/passports/engine-health-agent.yaml` (EngineHealthAgent passport); `docs/governance/ENGINE-FLEET-SPRINT-B.md` (writeup + ORCHESTRATION-NOTICE).
- **#1004 provenance compliance:** openclaw(npm steipete/vincentkoc)/aider(pipx aider-chat NOT aider)/metaclaw(pipx) = trusted, idempotent install. mirofish = build-from-local ~/MiroFish (docker-compose/npm; verified files present; LOCAL-ONLY, no registry substitute). nanoclaw = [BLOCKING: operator] comment, NO install (publisher unverified). hermes = BLOCKED comment, NO install (BANXE canon role, not public package). ironclaw = BLOCKED comment, NO install (wrong publisher kumareth). Blocked/blocking = escalation only, no install command.
- **Access:** engines-access.sh links only INSTALLED engines into ~/bin/engines (excludes blocked/blocking by construction); engines-status prints bin+version. Solves "engines hard to find". Installs nothing.
- **EngineHealthAgent passport (required-complete + fleet-convention; strict `additionalProperties` NOT enforced fleet-wide):** required fields present (agent_id/name/version/level/trust_zone/capabilities/ports/bounded_context/invariants/governance). trust_zone=GREEN, level=1, autonomy=L1_AUTO (read-only/observe ADR-128), human_double=Head of AI Platform, reports_to=CTO, hitl_gate=none, tasks=[health-check every [RATIFY]min, alert operator, ledger log]. Invariants: READ-ONLY never install/start/stop/manage; no authority expansion; blocked engines expected-absent; no auth bypass. status=PROPOSED (not activated — activation = operator ADR-135 gate #989); kept in config/agents/passports/ (prepare-only, NOT injected into canonical agents/passports/ fleet).
- **Health-check:** engine-health-check.sh = one-shot read-only (bin + /dev/tcp port, no auth), ALERT on down; does NOT daemonise (7/24 = operator systemd/cron).
- **Boundaries:** 5 new files + this shard. 0 installs, 0 daemons; no invented source; #1004 respected; no auth bypass; no legal/ss1/GUYON/ADR/perimeter/compliance-config; no passport outside engine-health-agent.yaml; Rule-6 dirty checkouts not touched. 0 off-scope.
- **Anti-dup (ADR-102) pointer-first:** references #992/#1004 (guardrail), #988 (liveness), #989 (status/activation), schemas/agent_passport.schema.json, ADR-128/126/148, CLAUDE.md §9, agents.md (MiroFish :3001) — restates none; scripts+passport+writeup, no code duplication, no repo move.
- **Scope/flow:** authored per #900 — files + paired shard ATOMIC; NO hand-edit of generated ledger; NO hardcoded IL (build_ledger mints, ADR-119 Rule 8). ⚠️ redis degraded → local mint; #1003/#1004 hold 844 → take max+1; if collision reset+re-mint (L-06), shard after reset (L-05).
- **Proof:** IL provisional (ADR-119 Rule 8) — max+1 over origin/main via allocator (ADR-143, DEGRADED local); unique at author time; 1:1 (ADR-144). Append-only: ONE tail shard, il_ts `2026-07-03T11:00:00Z` > main max. Fresh worktree off origin/main (ADR-120/060). FROZEN/.canon untouched. Passport required-fields-complete + fleet-convention-conformant (strict additionalProperties:false NOT enforced fleet-wide; matches the de-facto fleet). parent_canon=CANONICAL-ORG-CHART-v2; ports in fleet inbound/outbound format.
- **Status:** DONE — 5 files + shard. **DRAFT PR; DO NOT MERGE — operator HITL. AWAITS-OPERATOR: (1) run install-engines.sh (dual-use, license-review ADR-148/§9); (2) trusted source for hermes/nanoclaw OR keep blocked; (3) ironclaw stays blocked; (4) activate EngineHealthAgent (ADR-135) + daemonise health 7/24.**
- **Refs:** `scripts/engines/{install-engines,engines-access,engine-health-check}.sh`; `config/agents/passports/engine-health-agent.yaml`; `docs/governance/ENGINE-FLEET-SPRINT-B.md`; #992/#1004 (guardrail); #988 (liveness); #989 (status); schemas/agent_passport.schema.json; ADR-128/126/148; CLAUDE.md §9; agents.md; ADR-102/119/143/144; #900. Operator directive 2026-07-03 (Sprint-B engine package; prepare-only; install nothing; respect #1004; no auth bypass).
