---
il_ts: 2026-06-23T00:20:00Z
session_id: agent-factory-governance-block-a-d-merge-queue-readiness
source: CTIO
status: REVIEW
---
### Reconcile repo-backed Block A (SMF roles) and Block D (OBIE/MRM architecture) items; verify merge-queue readiness artifacts for operator activation

- **Instrukciya:** Narrow reconciliation sprint — close only repo-backed / operator-decided items in
  Block A (SMF roles / FCA governance ownership) and Block D (OBIE / Open Banking / MRM architecture);
  verify merge-queue readiness artifacts for operator activation. Operator supreme over docs; invent no
  facts; unknowns stay AWAITS OPERATOR. DO NOT MERGE.

- **Shagi:**
  1. Built evidence map from repo files only (read-only).
  2. Reconciled the GLOSSARY stale-negative entries whose source-of-truth now exists on `main`
     (per the GLOSSARY's own §12 reconciliation protocol).
  3. Verified merge-queue artifacts against the live workflows + actual `main` branch protection.
  4. Appended this shard; regenerated INSTRUCTION-LEDGER.md.

- **Evidence map (repo-backed):**
  - *Block A — SMF roles.* SMF framework already asserted: SMF1/SMF2/SMF4/SMF5/SMF17/SMF24/SMF26 in
    `docs/JOB-DESCRIPTIONS.md` + `docs/ORG-STRUCTURE.md`. RACI roles asserted in
    `docs/governance/MODEL-RISK-MANAGEMENT.md` §7 (CRO=SMF4, MLRO=SMF17, CTIO).
  - *Block D — OBIE / Open Banking.* `docs/governance/OPEN-BANKING-API-MANAGEMENT.md` (O-1..O-11);
    `docs/governance/GLOSSARY.md` §9 (PSD2/TPP/AISP/PISP/CBPII already grounded).
  - *Block D — MRM.* `docs/governance/MODEL-RISK-MANAGEMENT.md` now on `main` (SR 11-7 / EBA structure).
  - *Block-A/D lettering:* the only repo "Block A/D" definition (`docs/blocks-sprint8.md`) maps
    A=Core Banking System, D=GL/Reconciliation Engine — it does NOT define a governance-block lettering
    for SMF / OBIE / MRM. The SMF/OBIE/MRM grouping is treated as the audit subject, not asserted as a
    "Block A/D" scheme → see AWAITS OPERATOR.

- **Resolved — Block D (MRM), repo-backed:**
  - `GLOSSARY.md` §6 "MRM (framework)": stale "no formal MRM framework document on `main`" → grounded to
    `docs/governance/MODEL-RISK-MANAGEMENT.md` (structure only; binding classifications/thresholds/validation
    ownership remain AWAITS OPERATOR).
  - `GLOSSARY.md` §6 "T1/T2/T3": noted a *proposed, non-binding* tier structure now exists (MRM §4);
    binding per-model classification + thresholds remain AWAITS OPERATOR.
  - `GLOSSARY.md` §12 open-items register updated to RECONCILED with residual-AWAITS-OPERATOR noted.

- **Resolved — adjacent stale-negatives surfaced by the same GLOSSARY audit (repo-backed):**
  - `GLOSSARY.md` §8 DORA four keys / Deployment frequency / Lead time / Change-failure rate / MTTR:
    stale "not defined on `main`" → grounded to `docs/governance/KPI-DORA-FRAMEWORK.md` (D-1..D-4, §2/§5);
    numeric target bands remain AWAITS OPERATOR.

- **Resolved — Block A (SMF roles):** NONE. The SMF framework is already fully asserted; every open SMF
  item is a genuine operator decision (named holders / new roles) → left AWAITS OPERATOR (no facts invented).

- **Remaining AWAITS OPERATOR (left untouched — genuine operator decisions):**
  - Block A: named holders for CRO (SMF4) / MLRO (SMF17) [TBC in JOB-DESCRIPTIONS]; dedicated API/Platform
    product owner distinct from SMF26 (OPEN-BANKING O-11); VP Platform Eng / SRE (KPI-DORA §6);
    Head of Design / Design System Lead (UI-UX OI-1); Head of Security Engineering / CISO (DEVSECOPS).
  - Block D (OBIE): O-1..O-11 (version scheme, backward-compat depth, sunset windows, deprecation policy,
    TPP onboarding, gateway runtime config, API-governor channel activation, …) — all CTIO/COO/operator.
  - Block D (MRM): binding T1/T2/T3 classification + FCA/EBA criticality thresholds; independent-validation
    gate + validator ownership + revalidation cadence; model-monitoring metric set/thresholds; ADR-031
    identifier reconciliation; `ai-heavy` route/backend divergence; doubled-dev composition (ADR-117 Q6).
  - Governance-block lettering: whether SMF=Block A and OBIE/MRM=Block D is the canonical scheme — not
    asserted in any repo doc; the only `blocks-sprint8.md` lettering means something different.
  - DORA: numeric target bands for D-1..D-4 (operator-approved bands reconciled on a separate branch, not
    yet on `main`).

- **Merge-queue readiness findings:**
  - `merge_group:` trigger present in BOTH `.github/workflows/ledger-build.yml` and
    `.github/workflows/guardian.yml`. ✅
  - The 4 required contexts in `docs/governance/merge-queue-ruleset.json` /
    `OPERATOR-ENABLE-MERGE-QUEUE.md` (`guardian-factory`, `guardian-project`, `ledger-append-only`,
    `guardian-ledger`) all exist as real workflow job names AND match the live `main` branch protection
    (verified read-only via `gh api .../branches/main/protection`). ✅ Artifacts are consistent — no
    wording change needed.
  - OBSERVATION (operator decision, not auto-applied): `guardian-ledger-shards` (ADR-059 S3 — the
    `generated == rebuild` gate central to the ledger-renumber race the queue exists to solve) and
    `ledger-build` are NOT among the required-4 contexts. Whether to add them to the merge-queue ruleset
    is an operator policy decision → AWAITS OPERATOR (no speculative edit made).

- **Proof:**
  - `git -C . diff --name-only origin/main` → `docs/governance/GLOSSARY.md`, `INSTRUCTION-LEDGER.md`,
    new shard under `ledger/entries/agent-factory-governance-block-a-d-merge-queue-readiness/`.
  - `python3 ledger/build_ledger.py --check` → `ledger-build check OK`.
  - `gh api repos/CarmiBanxe/banxe-architecture/branches/main/protection --jq '.required_status_checks.contexts'`
    → `["guardian-factory","guardian-project","ledger-append-only","guardian-ledger"]`.

- **Deviation:** Included DORA four-key glossary stale-negatives (adjacent to Block A/D scope) because they
  are pure, demonstrably-false "not defined on `main`" claims whose source-of-truth now exists on `main`;
  fixing them is higher-integrity than leaving a false claim. No facts invented.

- **Blocker:** none.

- **Refs:** GLOSSARY §12 reconciliation protocol; `docs/governance/MODEL-RISK-MANAGEMENT.md`;
  `docs/governance/KPI-DORA-FRAMEWORK.md`; `docs/governance/OPERATOR-ENABLE-MERGE-QUEUE.md`;
  `docs/governance/merge-queue-ruleset.json`; ADR-056/057/059/059-A/060; `docs/blocks-sprint8.md`.
