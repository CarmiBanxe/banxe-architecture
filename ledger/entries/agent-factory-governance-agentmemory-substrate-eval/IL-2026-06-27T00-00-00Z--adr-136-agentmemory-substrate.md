---
il_ts: 2026-06-27T00:00:00Z
session_id: agent-factory-governance-agentmemory-substrate-eval
source: CEO
status: DONE
---
### ADR-136 — agentmemory shared-memory substrate (Factory fork vs Project fork, sensitive-domain OUT OF SCOPE, gated rollout)
- **Decision:** Created `docs/adr/ADR-136-agentmemory-shared-memory-substrate.md` (PROPOSED, concept_only) evaluating/integrating `rohitg00/agentmemory` (external ref, **NOT imported**) canon-safely. agentmemory = Tier-1 **read-only-w.r.t.-authority shared memory substrate** for **factory** agents (Claude Code/OpenClaw/Hermes per ADR-126/127). **Two forks (ADR-117 perimeter):** Factory fork (factory node, factory context — only one enabled at adoption) vs Project fork (separate deployment, **disabled by default**, non-sensitive/non-prod only); forks share no store, memory never crosses the perimeter. **OUT OF SCOPE by default** (no capture/replay without separate ADR + operator + IronClaw sign-off): payment-core, KYC, AML, sanctions, ledger, secrets-bearing flows, customer PII (RED zone AI-FORBIDDEN). **Mandatory boundaries:** redaction-at-capture (deny-by-default, fail-closed), bounded retention+purge (config-as-data, operator-owned), replay scoped to same fork+perimeter (observability not re-execution), **read-only w.r.t. authority** (memory describes never authorizes — ADR-130/127; recalled item provisional, re-verified). **Rollout phases (gated):** Factory-only → Non-prod project (operator approval) → gated project expansion (**ADR-135 held-out validation gate** + operator + IronClaw; sensitive domains still need their own ADR). Fail-closed throughout.
- **ADR-102 Duplication Audit:** **NO prior agentmemory/rohitg00 reference** (`git grep origin/main` = none); no shared-memory-substrate ADR. Adjacent memory canon distinct → KEEP + cross-link: CMS (SKILLS-MATRIX Skill 1 = process-level IL continuity/handoff, not a deployed store), ADR-059 (per-session ledger shards = canonical memory of record, unchanged/authoritative), ADR-098 (sandbox session recorder/replay = different observability surface), ADR-126/127 (Hermes Tier-1 3-tier memory/read-only), ADR-135 (held-out gate). agentmemory = new queryable shared substrate over these — **additive, not a rewrite**; ledger stays source of truth, substrate carries no authority. No code/plugin/server/hook/MCP/dataset imported; no `.env`/secret. ADR # = **136** (origin/main max 135). **Docs/governance only** — repo has no MCP/memory config surface (config/ = traffic-light.env only), so per task point 3 NO config stub added.
- **Proof:** docs/governance-only; concept_only; **no runtime, no secret handling, no authority expansion**. Append-only (ADR-059-A): ONE tail shard, il_ts `2026-06-27T00:00:00Z` strictly > origin/main max `2026-06-26T23:50:00Z`. IL **provisional, NOT hardcoded** (ADR-119 Rule 8) — `build_ledger` mints max+1 over current `origin/main` (max 559) → IL-560; frozen at rebase-before-merge. Validated by live ADR-133 global-uniqueness gate (540 allowlisted). Isolated worktree off origin/main `f489b33` (ADR-120); namespace ADR-060; existing ADR/souls/CMS untouched; no git ops outside this branch.
- **Status:** PROPOSED — ADR-136 candidate (governance/concept-only). Squash PR to main; DO NOT MERGE — STOP before merge, operator HITL.
- **Refs:** `docs/adr/ADR-136-agentmemory-shared-memory-substrate.md` (NEW); external `github.com/rohitg00/agentmemory` (ref, not imported); ADR-126/127 (Hermes Tier-1), ADR-130 (no authority expansion), ADR-135 (held-out gate), ADR-098 (sandbox replay), ADR-059 (ledger shards), ADR-117 (perimeter), ADR-102; `docs/SKILLS-MATRIX.md` Skill 1 CMS. Squash PR — operator HITL.
