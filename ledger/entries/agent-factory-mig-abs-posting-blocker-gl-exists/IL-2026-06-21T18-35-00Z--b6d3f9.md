---
il_ts: 2026-06-21T18:35:00Z
session_id: agent-factory-mig-abs-posting-blocker-gl-exists
source: CEO
status: BLOCKED
---
### MIG-ABS-posting BLOCKER — double-entry GL/posting via LedgerPort уже существует в banxe-emi-stack (mandatory preflight + ADR-102, no scaffold)
- **Instrukciya:** ABS delta abs-posting → ledger via LedgerPort → banxe-emi-stack. ОБЯЗАТЕЛЬНЫЙ read-only preflight (services/ + api/ на abs-posting/posting/ledger_port); если posting-surface уже есть → STOP scaffold, blocker + IL (BLOCKED) + covered/reconcile per discipline. Только фабрика; isolated worktree (Rule 1/6); docs-only PR без merge; IL append-only (ADR-059-A), il_ts строго выше re-fetched max; ADR-102 HARD RULE. global fence-check (урок M2.5-BIF). Midaz LedgerPort не вызывать напрямую (через port), ledger.py не дублировать (ADR-013 canonical SoT).
- **Preflight outcome (read-only, origin/main 3228d3d):** posting/double-entry/GL subsystem УЖЕ полностью реализован в services/ledger/: ledger_models.py — Posting (debit/credit, Decimal I-01, immutable I-24) + PostingDirection (debit/credit) + PostingStatus + JournalEntry (invariant sum debits == sum credits per currency) + HIGH_VALUE_THRESHOLD; gl_service.py — GLService (IL-FIN-01) post_journal_entry() consume LedgerPort (from services.ledger.ledger_port import LedgerPort) + UnbalancedEntryError (debits!=credits) + high-value approval = именно "abs-posting → ledger via LedgerPort"; payment_posting_service.py (PaymentEvent → PostingRule) + posting_rules.py (PostingRuleEngine, PostingRule debit/credit account types); midaz_adapter.py (Midaz LedgerPort impl, ADR-013) + inmemory_ledger.py. Consumers: api/routers/ledger.py, api/deps.py, services/recon/midaz_reconciliation.py.
- **Mismatch/duplication:** предложенный AbsPostingPort (ABC) + posting intent/entry DTO (double-entry debit/credit legs balanced) + LedgerPort consumption УЖЕ существует как GLService.post_journal_entry() + Posting/JournalEntry (balanced double-entry) + payment_posting_service (rules) + LedgerPort/Midaz adapter. Новый posting port = дубликат GL subsystem → ADR-102 violation (existing surface имеет registered consumers + tests). Midaz = single ledger SoT (ADR-013); GLService = canonical posting seam via LedgerPort. M2.5 ABS reconcile listed abs-posting → re-home ledger/Midaz via LedgerPort (not 2nd store); preflight подтвердил home УЖЕ существует (GLService + posting subsystem) → covered, не green-field scaffold. Per mandatory preflight + ADR-102 + M2.4/M2.5/M2.3/M2.4a precedent → STOP, no scaffold.
- **Distinction (что реально отсутствует):** Present (не дублировать): double-entry GL posting via LedgerPort (GLService), posting models, posting-rule engine, Midaz adapter. Possibly absent (единственный candidate): ABS-specific PostingRule (legacy ABS payment-event → GL debit/credit account types) = config/rule addition consuming существующий PostingRuleEngine + GLService, НЕ новый posting port / второй ledger.
- **Proof:** Decision required (operator): A declare-covered (mirror M2.4a; abs-posting satisfied by GLService/posting subsystem IL-FIN-01; legacy ABS posting retire-after; no scaffold); B thin ABS posting-rule (mirror M2.4-INT; ABS PostingRule legacy ABS payment-event → GL debit/credit consuming PostingRuleEngine+GLService, advisory, no new port, no 2nd ledger, no live posting — только если distinct ABS posting mapping нужен); C reconcile/gap-audit (ADR-102 audit legacy abs-posting.service vs GLService → delta + keep/merge/retire). Что сделано (read-only): mandatory preflight (services/+api/ grep) + классификация GL/posting subsystem (IL-FIN-01); подтверждены LedgerPort + ledger.py (Midaz live SoT) consume-target; pre-check global sys.modules fence-tests (M2.5-BIF lesson); blocker-doc + IL-shard (isolated worktree Rule 1/6). НЕ сделано: никакого scaffold (ни в одном репо); никакого AbsPostingPort/posting DTO; никакого backend PR; banxe-emi-stack НЕ ТРОНУТ (0 mig-abs-posting factory branches); ledger.py/Midaz не тронуты; KYC carve-out не тронут; merge не делал. Advisory-only blocker-report; shell strictly read-only (grep/find на worktree, gh ls-remote); append-only IL (il_ts 17:25 > re-fetched max 2026-06-21T17:10:00Z); build_ledger --check exit 0; docs-only PR; status=BLOCKED.
- **Status:** MIG-ABS-posting scaffold заблокирован (GL/posting subsystem уже есть, IL-FIN-01). Требуется operator-решение §4 (recommend A declare-covered, или B thin ABS posting-rule если distinct mapping нужен). Backlog correction: abs-posting = already in emi-stack (GLService/posting subsystem, IL-FIN-01). KYC/KYB/AML — только после I-27 sign-off.
- **Recommended next:** operator резолвит §4 (recommend A declare-covered / B thin ABS posting-rule / C reconcile); затем следующий ABS delta (scoring/agreement/contract/credential/legal-entity/info-field port — preflight-first) или abs-customer re-home → identity (consume existing customers surface). KYC/KYB/AML — только после I-27 sign-off; M2.8 frontend — после roster audit.
- **Refs:** docs/migration/MIG-ABS-posting-BLOCKER-gl-service-already-exists.md; read-only origin/main 3228d3d banxe-emi-stack services/ledger/{ledger_models,gl_service,posting_rules,posting_models,payment_posting_service,midaz_adapter,ledger_port}.py + api/models/ledger.py (Midaz live SoT) + api/routers/ledger.py; MIG-M2.5 (ABS reconcile — abs-posting re-home target), MIG-M2.4a (declare-covered precedent), MIG-M2.4-INT (thin-integration precedent); ADR-013, ADR-102, ADR-103, ADR-059-A, I-01, I-24, I-28; /tmp/banxe-migration-mapping-v0.claude.txt.
