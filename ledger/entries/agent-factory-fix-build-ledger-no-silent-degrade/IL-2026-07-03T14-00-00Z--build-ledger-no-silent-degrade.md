---
il_ts: 2026-07-03T14:00:00Z
session_id: agent-factory-fix-build-ledger-no-silent-degrade
source: CEO
status: DONE
---
### [OWNER: A] build_ledger fail-loud: no silent local-degrade + default REDIS_HOST=evo1 (fixes IL-thrash trap)
- **Decision:** Per operator, fixed the latent silent-degrade trap in `ledger/build_ledger.py`. Two changes: (Variant 2) `_redis_config` default `REDIS_HOST` `127.0.0.1`→`100.68.102.48` (evo1), aligning code with its own `_redis_allocate` docstring AND the existing test `test_targets_evo1_not_localhost` (which the `127.0.0.1` regression had made fail); (Variant 1) `_alloc_next` now FAILS LOUD (RuntimeError) on any redis failure instead of silently falling back to local max+1 — the offline local counter is available ONLY via explicit `BANXE_IL_ALLOCATOR=local`. Root cause of this session's IL-thrash: #990 regressed the default to `127.0.0.1`, so mints from Legion (no local redis) silently degraded to a per-host local counter → cross-terminal collisions (835→837→838/844). **PREPARE-ONLY**, review-first (NOT committed/pushed by factory). Owner A.
- **Diff (code):** `_redis_config` host default → `100.68.102.48`; `_alloc_next` except-branch raises `RuntimeError` (names host, cause, the `REDIS_HOST=100.68.102.48 + REDIS_PASSWORD` fix, and the `BANXE_IL_ALLOCATOR=local` escape hatch) instead of `WARN + return current_max+1`; docstrings of `_alloc_next` / `_redis_allocate` updated to fail-loud.
- **Diff (test):** `tests/test_redis_il_allocator.py` — `TestAllocatorFallback`→`TestAllocatorFailLoud`; `test_fail_loud_on_redis_unavailable` (asserts RuntimeError with `REFUSED` + `BANXE_IL_ALLOCATOR=local`, NOT local-fallback); `test_fail_loud_error_names_target_host` (RuntimeError names evo1 host); `test_explicit_local_mode` retained (local override → local, no raise); `test_targets_evo1_not_localhost` now passes; removed now-unused `io`/`redirect_stderr` imports. 10 tests pass.
- **Preserved:** `--check` stays offline/deterministic (`_alloc_next` never called on `--check`; `test_check_never_touches_redis` green); explicit `BANXE_IL_ALLOCATOR=local` path unchanged; monotonicity/seed/concurrent-mint tests unchanged and green.
- **Boundaries:** ONLY `ledger/build_ledger.py` + `tests/test_redis_il_allocator.py` + this shard. NO other code; NO secret read/printed; NO GUIYON; NO relocation; NO mint/commit/push by factory (review-first). semgrep on changed files adds 0 new findings (pre-existing sha1 at L71/83 untouched). 0 off-scope.
- **Anti-dup (ADR-102):** amends the one allocator function + its test in place; no parallel allocator, restates none.
- **Scope/flow:** authored per #900 — code+test+paired shard. NO hardcoded IL (build_ledger mints redis-serialized at operator's credentialed step, ADR-119 Rule 8). Re-mint/collision discipline: L-05/L-06.
- **Proof:** IL provisional — assigned by the operator's redis-serialized mint (REDIS_HOST=100.68.102.48, REDIS_PASSWORD, BANXE_IL_ALLOCATOR unset), max+1 over origin/main. Append-only tail shard, il_ts `2026-07-03T14:00:00Z`. Fresh worktree off origin/main (ADR-120). FROZEN/.canon untouched.
- **Status:** REVIEW — diff + test ready; NOT committed/pushed. **Operator mints IL redis-serialized + commits + pushes + merges via the standard recipe.**
- **Refs:** `ledger/build_ledger.py`; `tests/test_redis_il_allocator.py`; #990 (the regression); ADR-143/143-A (redis allocator); ADR-119; ADR-104 §5 (was graceful-degrade, now fail-loud); [[redis-il-counter-mint-procedure]]; #900. Operator directive 2026-07-03 (fail-loud, no silent degrade; align default).
