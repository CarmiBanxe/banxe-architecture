---
il_ts: 2026-06-24T19:30:00Z
session_id: agent-factory-factory-s2-devsecops-activate
source: CEO
status: DONE
---
### S2 DevSecOps — activate executable security controls (SAST/SCA/SBOM/signing) as advisory CI; close the lead executable conformance gap
- **Decision:** Closed the S2 DevSecOps executable gap (conformance audit #742, ~79%, S2 = lead executable gap) by wiring the four security controls that `docs/governance/DEVSECOPS-SSDLC.md` names — implementing exactly what the canon specifies, inventing nothing. All four run on `pull_request` + `push` to `main` (+ `workflow_dispatch`) so they EXECUTE and can prove green, and ALL are **ADVISORY (non-blocking)**: none is added to branch protection / required status checks (operator-reserved — they must prove green first). Canon §3 ("S2 adds no required gate") preserved; no existing guardian weakened.
- **Tools (verbatim from DEVSECOPS-SSDLC.md §4/§5, RECONCILED 2026-06-22 — no tool invented):** SAST = **CodeQL** (`codeql.yml`, language python); SCA = **OSV-Scanner** (`osv-scanner.yml`, recursive dep+fs — the executable half of the canon "Dependabot + OSV-Scanner"; Dependabot half = GitHub repo-setting toggle, operator-reserved §9 O-2); SBOM = **syft → CycloneDX** (`sbom.yml`, promoted from `sbom.yml.template`); container/artifact signing = **Cosign keyless/OIDC** (`cosign-sign.yml`, promoted from `cosign-sign.yml.template`; real `cosign sign` step operator-gated on `release` + repo var `COSIGN_SIGN_TARGET`, no key material in repo per ADR-103). The SCA task hint "Trivy/Grype" was overridden by canon supremacy (implement only what DEVSECOPS-SSDLC.md specifies → OSV-Scanner). DAST (OWASP ZAP), SLSA provenance, per-service STRIDE models, DORA workflow remain GAP/AWAITS OPERATOR (out of this pass).
- **Basis:** conformance audit `docs/roadmap/TARGET-MODEL-CONFORMANCE-2026-06-24.md` (#742, S2 lead executable gap) + governance canon `docs/governance/DEVSECOPS-SSDLC.md` (§2 phase→control map, §4 GAPS G-S2-01/02/04/05, §5 tooling stack — operator-approved selections RECONCILED 2026-06-22).
- **Implementation (minimal-diff):** 4 new active workflows under `.github/workflows/` (codeql.yml, osv-scanner.yml, sbom.yml, cosign-sign.yml); 2 templates removed (`sbom.yml.template`, `cosign-sign.yml.template`) as superseded by their active forms; action versions pinned (Config-over-Hardcoding §10: codeql-action@v3, osv-scanner-action@v1.9.1, sbom-action@v0.17.8, cosign-installer@v3.7.0). `docs/governance/DEVSECOPS-SSDLC.md` updated PARTIAL→ACTIVE for the now-wired controls only (status header, current-maturity, §2 rows, new §3.6 advisory-scanner inventory, §4 register rows G-S2-01/02/04/05 + activation note, §5 tooling rows, §10 revision note) — reflects reality only; DAST/SLSA/threat-models/DORA left as GAP.
- **Proof:** all five workflow YAML parse clean (`yaml.safe_load` OK: codeql.yml, osv-scanner.yml, sbom.yml, cosign-sign.yml, ci.yml). Append-only (ADR-059-A): ONE new tail shard at il_ts `2026-06-24T19:30:00Z` (strictly > fresh tree max `2026-06-24T17:30:00Z`); prior shards + INSTRUCTION-LEDGER history untouched; INSTRUCTION-LEDGER.md + IL-SEQUENCE.json regenerated via `python3 ledger/build_ledger.py`, `--check` exit 0. Isolated worktree off origin/main (ADR-120); shared checkouts / parallel worktrees untouched (Rule 6). Diff scoped to: 4 new workflows + 2 template deletions + DEVSECOPS-SSDLC.md + shard + INSTRUCTION-LEDGER.md + IL-SEQUENCE.json — no pollution/.quarantine//docs/incoming.
- **Status:** S2 DevSecOps executable controls = ACTIVE (advisory). Maturity advanced PARTIAL→ACTIVE for SAST/SCA/SBOM/signing; gate posture unchanged (no required gate). DO NOT MERGE pending operator review — squash-merge operator-reserved.
- **Recommended next (operator):** after the four advisory checks prove green, mark REQUIRED in branch protection — `codeql-analyze (python)`, `osv-scanner-scan`, `sbom-cyclonedx`, `cosign-sign`; then decide O-2 (Dependabot toggle), O-5/O-7 (cosign sign target + identity policy), CodeQL on-prem self-hosted runner switch (ADR-031), and the remaining gaps (DAST/OWASP ZAP, SLSA, STRIDE instances, DORA workflow).
- **Refs:** `docs/governance/DEVSECOPS-SSDLC.md`; `docs/roadmap/TARGET-MODEL-CONFORMANCE-2026-06-24.md`; `.github/workflows/{codeql,osv-scanner,sbom,cosign-sign}.yml` (NEW); removed `.github/workflows/{sbom,cosign-sign}.yml.template`; ADR-031 (on-prem), ADR-059-A (append-only ledger), ADR-102 (anti-dup), ADR-103 (no key material/server-only), ADR-120 (isolated worktree), CLAUDE.md §10 (Config-over-Hardcoding); grounding `.github/workflows/{ci,guardian,ledger-build,docs}.yml`.
