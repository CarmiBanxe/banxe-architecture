---
il_ts: 2026-06-27T20:45:00Z
session_id: agent-factory-governance-merge-serialize-gate
source: CEO
status: DONE
---
### Merge serialization fallback (Variant B) — concurrency-group + base-drift guard (native merge queue unavailable on user-repo)
- **Decision:** Added `.github/workflows/main-serialize.yml` — software serialization of merges into `main` for this **user-owned** repo where the native GitHub merge queue is **org-only / unavailable** (REST 422; UI won't persist; transfer blocked — account has no org). Closes the **second** root of the IL-dup/DIRTY problem — the **merge race / base-drift** (#832, #825 went DIRTY on parallel work) — complementing the Redis central allocator (ADR-143/143-A) which closed the ID-collision root. **Mechanism:** (1) `concurrency: {group: "main-merge-serialize", cancel-in-progress: false}` → gates queue in Actions one-PR-at-a-time (soft serialization); (2) **base-drift guard** — checkout PR head, fetch origin/main, `BEHIND=git rev-list --count HEAD..origin/main`; if `BEHIND>0` → **fail** with `BASE DRIFT: PR is N commits behind origin/main — rebase via hard-reset recipe before merge`; logs origin/main + PR-head + merge-base SHAs + behind/ahead; `behind==0` ⇒ exit 0. `on: pull_request [opened,synchronize,reopened,ready_for_review] branches:[main]`.
- **Operator follow-up (settings, NOT code):** add context `main-merge-serialize` to branch-protection required status checks (keep strict up-to-date) → makes the guard a hard merge gate. Documented in `docs/governance/MERGE-SERIALIZATION-FALLBACK.md` incl. upgrade-path (transfer→org ⇒ native merge-queue supersedes, remove fallback).
- **Quality + dup checks:** workflow YAML valid (pyyaml; job/name `main-merge-serialize`, concurrency group + cancel-in-progress:false present); semgrep 0; existing job-context names unchanged. IL **provisional, NOT hardcoded** (ADR-119 Rule 8) — `build_ledger` mints max+1 over current `origin/main` (max 616) → IL-617 via central allocator (ADR-143/143-A); unique, 0 new minted-value dups ({540:2} unchanged), 0 new body-heading dups.
- **Proof:** CI+docs change; **no ledger/FROZEN-ARCHIVE/prior-ADR bodies touched** (coupling shard required because `docs/governance/` is NOT in guardian INFRA_RE / ADR-056). Append-only (ADR-059-A): ONE tail shard, il_ts `2026-06-27T20:45:00Z` strictly > origin/main max `2026-06-27T20:30:00Z`. `build_ledger --check` exit 0 offline. Isolated worktree off origin/main `bc8299d` (ADR-120); namespace ADR-060.
- **Status:** DONE — workflow + governance doc + shard. PR; DO NOT MERGE — STOP, operator HITL. After merge operator adds `main-merge-serialize` to required checks.
- **Refs:** `.github/workflows/main-serialize.yml`; `docs/governance/MERGE-SERIALIZATION-FALLBACK.md`; ADR-143/143-A (allocator), `LEDGER-MERGE-QUEUE.md`/`merge-queue-ruleset.json` (native queue, unavailable), ADR-119/057/059-A. Operator HITL merge.
