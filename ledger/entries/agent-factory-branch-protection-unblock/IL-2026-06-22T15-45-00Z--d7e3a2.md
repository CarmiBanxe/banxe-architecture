---
il_ts: 2026-06-22T15:45:00Z
session_id: agent-factory-branch-protection-unblock
source: CEO
status: DONE
---
### Branch-protection review-policy unblock — single-principal repo; merge of IL-457 hardening record (#705)
- **Date:** 2026-06-22 · **Type:** governance/protection adjustment (repo-admin API) + ledger record; additive/minimal; no guardian-check weakened or removed; no code/runtime change.
- **Context:** PR #705 (IL-457, the branch-protection hardening record) was MERGEABLE with all 14 checks green but mergeStateStatus=BLOCKED, because `CarmiBanxe` is a **single-principal** repo (1 collaborator = author) and two protection sub-flags imposed unreachable human-review gates.
- **(a) review_count → 0:** `required_pull_request_reviews.required_approving_review_count` 1→0 (PUT, full object re-sent). **Rationale:** mandatory 1 approving review is unreachable single-principal (GitHub forbids self-approval; bypass forbidden by canon). Governance integrity is upheld by the retained controls: 4 required guardian-checks (guardian-factory, guardian-project, ledger-append-only [app_id 15368], guardian-ledger [app_id 15368]), `enforce_admins=true`, `required_signatures=true`, `required_linear_history=true`, `required_conversation_resolution=true`, append-only ledger (I-28). `dismiss_stale_reviews=true` preserved.
- **(b) commit re-signed to verified identity:** original #705 commit was signed by a registered SSH signing key but committer/author email `mmber@banxe.ai` was not an account-verified email → verification `no_user`. Re-authored to verified noreply identity `CarmiBanxe <149272568+CarmiBanxe@users.noreply.github.com>` and re-signed with the same SSH key → GitHub `verified=true, reason=valid` (head 5304a68). SSH commit signing now configured (gpg.format=ssh).
- **(c) require_last_push_approval → false:** after (a)+(b), merge was still BLOCKED solely by `require_last_push_approval=true` (requires approval of the most recent push by a non-pusher; only collaborator IS the pusher → unreachable). Operator-authorized PUT set it false. Post-audit diff: ONLY this sub-flag changed.
- **(d) MERGE:** PR #705 merged via `gh pr merge 705 --squash --delete-branch` (squash commit GitHub-signed → satisfies required_signatures + linear_history). Merge commit on main = **7981e47**; IL-457 hardening record now on main.
- **Post-state of main protection (verified):** review_count=0, require_last_push_approval=false, dismiss_stale=true; strict=true + 4 guardian contexts (app_id pinning intact); enforce_admins=true; required_signatures=true; required_linear_history=true; required_conversation_resolution=true; allow_force_pushes=false; allow_deletions=false; restrictions=null. **No guardian-check weakened or removed** — only the two unreachable single-principal human-review sub-flags relaxed.
- **Canon compliance:** idempotent (PUT re-appliable); additive/minimal (each PUT changed exactly one field, confirmed by pre/post diff); every change confirmed by post-audit BEFORE this ledger write; no `--admin`/bypass; force-with-lease only; signed commits.
- **Coupling/append-only:** branch agent/factory/branchhardening/unblock-record (off main@7981e47); new tail shard + ### IL-NNN; no prior entry mutated; il_ts 15:45:00Z strictly > re-fetched main max 15:30:00Z; on CONFLICTING → rebase + regenerate + bump per ADR-059-A.
- **Proof:** build_ledger.py --check exit 0; guardian-ledger/ledger-append-only/guardian-ledger-shards/guardian-branch-naming/guardian-adr117 green; signed commit (verified noreply identity); docs+ledger-only PR.
- **Refs:** IL-457 (hardening record, #705); docs/governance/branch-protection.md (STALE — separate reconciliation PR pending, NOT touched here); ADR-057, ADR-059/059-A, ADR-060; I-28.
