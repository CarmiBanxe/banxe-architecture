---
il_ts: 2026-06-21T22:30:00Z
session_id: agent-factory-mig-login-history
source: CEO
status: DONE
---
### MIG login-history — advisory login-audit surface (genuine-gap #2, code substep) — BANXE.RAR → EMI
- **Instrukciya:** MIG genuine-gap #2 (из coverage-audit IL-411/412): login-history → banxe-emi-stack. ОБЯЗАТЕЛЬНЫЙ read-only preflight (login.history/login_event/LoginHistory в services/auth+api/; existing auth-audit surface?); если canonical surface → blocker/findings, STOP. Если чисто → scaffold advisory/sandbox: LoginHistoryPort + DTO (login_event/timestamp[param]/masked_ip/user_ref/outcome/source) + LoginOutcome enum + sandbox provider fail-closed; consume/extend existing auth (НЕ дубль AuthApplicationService); no live auth, PII masked, timestamp caller-supplied (no Date.now/wall-clock), I-01 no-float; characterization+contract+fence tests + global-fence-check (M2.5-BIF); ADR-102; CI-gated (9-check, remediation in-branch без bypass при fail); paired backend-PR + arch IL; merge не делать. Server-only evo1 isolated worktree (ADR-103, Rule 1, branch verified).
- **Preflight (read-only, origin/main 72334ce):** emi-stack — НЕТ existing login-history surface (exact grep login.history/login_history/loginhistory/audit.login/login_event/LoginHistoryPort пусто; нет class/port токенов). rate_limit_audit_emitter = rate-limit audit, иной concern (не login-history). Genuine-gap подтверждён. AuthApplicationService.login() (services/auth/auth_application_service.py:121) = consume/extend context (не дублировать). Legacy источник: banxe-fiat-backend/banxe-auth-backend/src/login-history/ (entity user_id/ip/geolocation/useragent/company_id + add-to-history.input + service + resolver). Нет login-history global sys.modules fence-тестов.
- **Scaffold (banxe-emi-stack, ветка agent/factory/mig-login-history от main 72334ce, isolated worktree /tmp/wt-lh-b):** services/auth/login_history.py — LoginHistoryPort (ABC) + LoginHistoryRecord (@dataclass: event_id, login_event, timestamp[caller-supplied], masked_ip, user_ref, outcome[LoginOutcome enum success/failure/mfa_required/locked/expired], source) + SandboxLoginHistoryProvider (config-as-data in-memory, record() masks ip + caller timestamp, fail-closed get_event→None) + mask_ip() PII helper (IPv4 last octet redacted, IPv6 first hextet, unknown→****). NO live auth/Midaz/KYC/KYB/AML/ledger mutation. PII: ip masked перед storage, user_ref opaque, no raw PII retained. Time: timestamp caller-supplied (модуль не вызывает wall-clock datetime.now/Date.now) → deterministic/replay-safe. I-01: no float. tests/test_login_history.py — characterization (port/DTO/LoginOutcome shape) + contract (record shape; timestamp param; mask_ip variants) + fence (0 midaz/ledger/kyc imports; no wall-clock calls; PII masked; no float usage). Existing auth surfaces (auth_application_service/sca_*/token_manager) UNCHANGED (0 в diff).
- **Proof:** Duplication Audit (ADR-102): additive auth-audit surface; preflight подтвердил НЕТ existing login-history surface; НЕ дубль AuthApplicationService/ScaApplicationService/token_manager — single auth bounded context, это audit sibling; consume existing auth context. KYC carve-out intact. Invariants/fences verified: no live auth/Midaz/KYC/ledger; fail-closed; PII masked (mask_ip перед storage, no raw ip); timestamp caller-supplied (no wall-clock); I-01 no-float. Global fence-check (M2.5-BIF): import login_history добавляет 0 bifrost/typeorm/redis/gcp в sys.modules (NONE). Diff = 2 файла (login_history.py + test), 0 existing-auth files. Валидация (evo1 isolated worktree): ruff check + ruff format --check clean; pytest tests/test_login_history.py 5 passed (--noconftest isolated); global-fence NONE. Remediation в той же ветке: fence wall-clock substring (docstring упоминал datetime.now/Date.now) → сужен до calls (datetime.now(/.utcnow(/date.now(), не bypass. ПОЛНЫЙ 9-check Quality Gate — впервые в CI (module test isolated локально; full suite в CI; при fail → remediation in-branch без bypass). Backend-PR banxe-emi-stack #208 OPEN (no merge). Server-only isolated worktree (Rule 1/6); existing auth surfaces/parallel-session ветки не тронуты; merge не делал.
- **Status:** login-history genuine-gap #2 scaffold готов (advisory/sandbox auth-audit); descriptive, PII-masked, no live auth, deterministic timestamp. honest CI-status (ruff+module-pytest+global-fence локально green, full 9-check впервые в CI).
- **Recommended next:** genuine-gap #3 — SRP (secure-remote-password login strategy за существующей auth surface; SECURITY-SENSITIVE — extra review; mandatory preflight → server-side → paired PR + arch IL → CI-gated → no merge). ИЛИ M2.4c-e OB delta. KYC/KYB/AML — только после I-27 sign-off; M2.8 frontend — после roster audit.
- **Refs:** banxe-emi-stack PR #208 (agent/factory/mig-login-history); services/auth/login_history.py + tests/test_login_history.py; semantic port для legacy banxe-fiat-backend/banxe-auth-backend/src/login-history/; coverage-audit MIG-ABS-identity (IL-411/412 — genuine-gap #2); reuse-not-dup AuthApplicationService/ScaApplicationService; sibling abs-info-field (gap #1, IL-412); ADR-102, ADR-103, ADR-059-A, I-01, I-28; /tmp/banxe-migration-mapping-v0.claude.txt.
