---
il_ts: 2026-07-02T04:00:00Z
session_id: agent-factory-evo2lane-b3r2-aider-and-gateway-addr
source: factory
status: DONE
---

### IL — close roadmap B3/R2 (evo2 agent-lane decision) + A17 gateway-address correction (docs-only)

**Scope:** governance / roadmap artefact — **docs-only**. Amends TWO existing
docs (no runtime, infra, LiteLLM, config, or secret change is authored by
this shard).

#### What this closes

1. **B3 / R2 / Sprint-E2** in `docs/roadmap/ERROR-RECONCILIATION-ROADMAP-2026-07-01.md`
   are marked **CLOSED / DONE 2026-07-02**. Decision recorded:
   **evo2 agent-lane = Aider-local** — Aider 0.86.2 installed on evo2 in
   `~/aider-venv` (Python venv), model `openai/factory-coder` routed via
   the governed **Legion LiteLLM at tailscale `100.101.218.26:4000`**.
   Validated LIVE 2026-07-02: **AIDER-OK**. **Lane A (Claude-quality:
   Anthropic API + Node / Claude-Code CLI) DEFERRED** — RED-zone /
   regulated code paths (payment, compliance, KYC, safeguarding) STAY
   on Claude per BUG-005 (Ruflo mandatory) and the agent-chain × GSD
   matrix in `.claude/rules/agents.md`. The Aider lane is scoped to
   local / non-regulated work only.

2. **A17 — LiteLLM gateway-address inaccuracy** is added to the §1
   ERROR REGISTER (status = corrected-here, owner = factory) and the
   confirmation footprint is updated to **A1–A17**. Earlier §5 / Aider
   guidance pointed at evo1's LiteLLM `100.68.102.48:4000`, but the
   canonical alias-gateway (whose config, master_key, and alias table —
   `factory-*` / `project-reason` — actually live there) is Legion's
   LiteLLM at tailscale `100.101.218.26:4000`. evo1's `:4000` is a
   different LiteLLM instance with a different key (auth rejected) —
   NOT the canonical alias-gateway. Additionally, OpenAI-compatible
   clients (Aider) must use the `openai/<alias>` model form so LiteLLM
   routes a bare alias through an OpenAI-compatible base.

3. **B2 row** notes evo2 now provisioned (Aider lane) with Lane A
   deferred; **C3 row** flips to "Aider lane installed + validated" with
   Lane A deferred.

4. **COMPUTE-ROUTING-TAXONOMY.md §5.3** correction: the imprecise line
   "evo1 runs LiteLLM on :4000" is replaced with a precise statement
   that the canonical alias-gateway (§1 alias table) is served by
   **Legion's LiteLLM at tailscale `100.101.218.26:4000`**; evo1's
   `100.68.102.48:4000` is a separate instance. A note is added that
   OpenAI-compatible clients (Aider) must use the `openai/<alias>` form.
   §1 alias table, §5.1 / §5.2 substrate distinctions, and the I-32 /
   I-33 no-bypass rule are **not** touched — all remain in force. A
   dated line records the correction: "correction 2026-07-02:
   alias-gateway = Legion `100.101.218.26:4000`".

#### Files amended

- `docs/roadmap/ERROR-RECONCILIATION-ROADMAP-2026-07-01.md` — B2 / B3 /
  C3 rows updated; A17 row added; confirmation footprint → A1–A17;
  §2 B3 section closed; §3 R2 phase closed; §4 Sprint-E2 closed.
- `docs/agent-engine-dossier/COMPUTE-ROUTING-TAXONOMY.md` — §5.3
  corrected (alias-gateway = Legion `100.101.218.26:4000`;
  `openai/<alias>` note; dated correction footer).

#### Invariants upheld

- **I-28** — append-only ledger; this shard adds one entry; no existing
  shard is edited or deleted.
- **I-32 / I-33** — no direct model bypass. §1 alias table, §5.1 / §5.2
  and the no-bypass rule are unchanged. All routing continues through
  the governed LiteLLM `:4000` proxy (the correction merely names the
  correct HOST — Legion, not evo1 — for the canonical alias-gateway).
- **ADR-102** — Duplication Audit: repo-wide check for the target
  amendments confirmed the two doc paths are the sole source-of-truth
  for (a) the roadmap register (B3 / R2 / Sprint-E2 / A17) and (b) the
  §5.3 gateway-address statement. No hidden consumers of the imprecise
  wording were found; decision per match = **update in place**. No new
  file introduced.
- **ADR-103** — authored on evo1 (server-only venue), not on an operator
  local machine.
- **ADR-119** — the IL number for this shard is minted by
  `build_ledger.py` (max+1 over current `origin/main`); never hardcoded
  in this shard body. `il_ts` is strictly greater than the current
  `origin/main` max.
- **ADR-060** — branch name `agent/factory/evo2lane/b3r2-aider-lane-and-gateway-addr`
  follows the actor / namespace canon.
- **BUG-005** — RED-zone (payment / compliance / KYC / safeguarding)
  explicitly re-stated as STAYS on Claude; the Aider lane is scoped to
  local / non-regulated work only. Ruflo remains mandatory middleware.

#### Safety

- **Docs-only.** Two doc amendments + one shard + regenerated ledger
  (`INSTRUCTION-LEDGER.md` + `IL-SEQUENCE.json`). No runtime, infra,
  LiteLLM, config, or secret mutation authored by this shard.
- **No secrets.** The LiteLLM master_key value does NOT appear anywhere
  in the amended text (only the non-secret gateway IP:port + alias
  names are recorded).
- **No foreign shard / branch touched** (parallel-session-isolation
  Rules 6 / 7).
- **0 ledger deletions.**
- **No merge.** This is a draft PR; operator arms auto-merge as a
  separate step.

#### Refs

- `docs/roadmap/ERROR-RECONCILIATION-ROADMAP-2026-07-01.md` (amended)
- `docs/agent-engine-dossier/COMPUTE-ROUTING-TAXONOMY.md` (§5.3 amended)
- `docs/adr/ADR-060-multi-actor-orchestration.md` (branch actor namespace)
- `docs/adr/ADR-102-no-smart-refactor-without-duplication-verification.md`
- `docs/adr/ADR-103-server-only-refactoring-policy.md` (venue)
- `docs/adr/ADR-119-stable-frozen-il-numbering.md` (IL freeze / re-mint)
- `.claude/rules/agents.md` (LiteLLM route map, BUG-005 Ruflo canon)
