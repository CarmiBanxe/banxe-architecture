---
il_ts: 2026-06-19T03:30:00Z
session_id: agent-factory-m1-supported-asset-breakdown-m1-23
source: CEO
status: DONE
---
### M1.23 supported-asset breakdown (flatten account supported_assets, code substep, additive/read-only) — BANXE.RAR → EMI
- **Instrukciya:** M1.23 code-подшаг: public read-only supported-asset breakdown — GET /api/v1/catalogue/supported-asset-breakdown → SupportedAssetBreakdown (by_supported_asset: SupportedAssetCount{asset,count} из flatten account_metadata().accounts[*].supported_assets, + total_accounts + total_memberships + source); третий list-flatten breakdown, завершает account-domain list-flatten сетку (после M1.22 capabilities), integer meta. accounts-per-asset (account-домен), НЕ второй asset-каталог; flatten-семантика (multi→N buckets, empty SYSTEM→none, memberships≠accounts). Distinct path /catalogue/supported-asset-breakdown. Server-only evo1 isolated worktree (Rule 1, branch verified перед commit); Factory-Only Execution ([CLAUDE CODE]); advisory/read-only; I-01 (integer meta counts; supported_assets=support-label, не held amount); fail-closed; Midaz LedgerPort/FeeEnginePort НЕ вызывать. Spec asset = non-exhaustive string (урок M1.16). Append-only IL-shard (ADR-059-A), il_ts строго выше re-fetched max по timestamp (02:45→03:30 после параллельного сдвига main до 03:15); rebase+regenerate при CONFLICTING; promotion-PR без merge (ADR-102).
- **Shagi:** banxe-trading-backend ветка agent/factory/m1/supported-asset-breakdown-m1-23 от main e0e992f (isolated worktree /tmp/wt-m123c). meta/breakdown.py: добавлены SupportedAssetCount(asset, count) + SupportedAssetBreakdown(by_supported_asset, total_accounts, total_memberships, source) + supported_asset_breakdown() (flatten account_metadata().accounts[*].supported_assets → per-asset account counts; deterministic sorted order; total_accounts=len(accounts), total_memberships=sum(counts); empty→no bucket; lazy SANDBOX_MOCK; reuse уже импортированного account_metadata). api/catalogue.py: новый GET /catalogue/supported-asset-breakdown на catalogue_router (distinct path); существующие endpoints не тронуты. docs/specs/catalogue-api.yaml: additive /v1/catalogue/supported-asset-breakdown path + SupportedAssetBreakdown/SupportedAssetCount schemas; SupportedAssetCount.asset = type:string non-exhaustive (canonical EUR/BTC примеры, без hard enum — урок M1.16); flatten-семантика + accounts-per-asset (не asset-каталог) задокументированы. Reuse account_metadata — без второго registry/counter; asset-каталог НЕ дублирован (считает assets/metadata, не accounts-per-asset). Midaz LedgerPort/FeeEnginePort НЕ вызваны (0 refs). Тесты tests/test_supported_asset_breakdown.py (+6): characterization (by_supported_asset == flatten group-by, total_accounts==len, total_memberships==sum==sum(len(supported_assets)), ЯВНЫЙ flatten-тест multi→N/empty SYSTEM→none/memberships≠accounts, deterministic order), contract (AccountAdvisoryMetadata(6)/AccountMetadataResponse(3) frozen + /accounts/metadata + /assets/metadata + /catalogue/* unchanged + asset-каталог не продублирован), fail-closed (empty → no bucket, нет фабрикованных ключей "/unknown/none", balance/posting/amount absent). Валидация (main venv via PYTHONPATH): ruff clean, mypy strict clean (85 files), pytest 489 passed (483+6); smoke GET /catalogue/supported-asset-breakdown 200 {bySupportedAsset BTC:1/CHF:2/ETH:1/EUR:2/GBP:2/USD:2/USDC:1, totalAccounts 3, totalMemberships 11 (11≠3: multi-asset accounts + empty SYSTEM — flatten подтверждён), source sandbox-mock}, /accounts/metadata + /assets/metadata 200 unchanged. Promotion-PR banxe-trading-backend #56 OPEN (no merge). IL-shard в banxe-architecture isolated worktree.
- **Proof:** Duplication Audit (ADR-102): account_metadata/AccountAdvisoryMetadata.supported_assets (M1.7) reuse — per-asset account counts flattened, no second registry/account-map; asset-каталог (assets/catalog.py) НЕ дублирован (считает assets/metadata, не accounts-per-asset — иная семантика); Midaz LedgerPort/FeeEnginePort НЕ вызваны (0 refs); meta/breakdown.py (M1.16–M1.22) reuse module same flatten pattern; SupportedAssetBreakdown/SupportedAssetCount = derived summary, НЕ data SoT; AccountAdvisoryMetadata/AccountMetadataResponse/breakdown DTO не мутированы (frozen). Verdict: zero data duplicates, derived flatten summary only, keep/extend, no merge/delete. build_ledger --check exit 0. Numeric (I-01): только integer meta counts (per-asset/total_accounts/total_memberships), НЕ balances/amounts/positions/ledger postings/prices/APY/computed fees; supported_assets = support-label не held amount; no money Decimal/BigNumber/float; no balance/posting surface, no fee computation. Activation Safety: read-only/advisory/mock-safe derived flatten summary; no live orders/execution/balances/positions/ledger postings/payments/wallet-custody/fund movement; NO live Midaz LedgerPort/FeeEnginePort calls; NO infra/secret/PII/ops metrics; no network/keys; fail-closed (counts из fail-closed account_metadata; empty → no bucket; no fabricated key). Spec-fidelity: asset non-exhaustive string, без hard enum; flatten-семантика + accounts-per-asset задокументированы. Grid-completion: account-domain list-flatten измерения исчерпаны; следующий домен потребует нового deep-read (возможна смена класса: meta/health enrichment). Out-of-scope: P0/regulated/live (orders/execution/balances/positions/ledger postings/wallet-custody/payments/live Midaz LedgerPort), APY/yield/rates re-surface, fee computation/billing, KYC/AML, cards, crypto-wallet ops, internal/ops/infra metrics/secrets/PII, второй asset-каталог, мутация frozen DTO, новый registry/counter source, user/DB migrations, live state machines, contract-breaking. Server-only в isolated worktree (Rule 1/6, Factory-Only Execution); fabric/prod/secrets/emi-stack/governance вне M1.23 не тронуты; чужие/parallel-session ветки/untracked не тронуты; branch protection не обойдён; merge не делал.
- **Refs:** banxe-trading-backend PR #56 (agent/factory/m1/supported-asset-breakdown-m1-23); src/banxe_trading_backend/meta/breakdown.py (supported_asset_breakdown), api/catalogue.py (GET /catalogue/supported-asset-breakdown), docs/specs/catalogue-api.yaml, tests/test_supported_asset_breakdown.py; reuse accounts/metadata.py account_metadata/AccountAdvisoryMetadata.supported_assets (M1.7 IL-272), meta/breakdown.py M1.21/M1.22 flatten pattern (IL-317/IL-321); ADR-013 (Midaz LedgerPort live SoT, untouched), ADR-102, ADR-103, ADR-059-A, I-01; docs/migration/M1.23-supported-asset-breakdown.md (plan, PR #564).
