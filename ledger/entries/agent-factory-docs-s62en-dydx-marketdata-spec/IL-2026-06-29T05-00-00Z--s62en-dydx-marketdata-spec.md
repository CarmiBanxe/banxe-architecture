---
il_ts: 2026-06-29T05:00:00Z
session_id: agent-factory-docs-s62en-dydx-marketdata-spec
source: factory
status: DONE
---
### S6.2-EN — enable dYdX MarketDataPort sandbox-live (enable-not-build, ADR-102 reuse) [docs-only spec]
- **Decision:** Created `docs/specs/S6.2-EN-DYDX-MARKETDATA-SANDBOX-LIVE-SPEC-2026-06-29.md` (PROPOSED, **docs-only**) — the verify-first enable-spec for road map Phase-1 **S6.2-EN**. ADR-094 note: S6.2-EN is **valid** (ADR-094 dropped only S6.6/S6.7).
- **ADR-102 dup-check (mandatory, done):** clean — no existing S6.2-EN spec in `docs/`; no existing market sandbox-live wiring in the trading-backend tree. Net-new spec.
- **Reuse-map (verified read-only, `banxe-trading-backend@main`):** `ports/dydx_market_data.py` (dYdX v4 Indexer adapter — API-only, no key, maps Indexer→§D2 envelope unchanged, synthesizes monotonic sequence, I-01 Decimal validation) and `ports/market_data_port.py` (`MarketDataPort` + §D2 contract IL-195) = **REUSE, do NOT rebuild**. The mock stays CI/default. **Net-new = only the flag-gate wiring** (`BANXE_DSE_PROVIDER_MODE`/`BANXE_DSE_LIVE_ALLOWED` are "future-spec — not implemented" per `dse-live-providers-options.md`), config-as-data, no adapter logic.
- **Flags (verified):** `BANXE_DSE_MARKET_PROVIDER=dydx` + `BANXE_DSE_PROVIDER_MODE=sandbox-live` + master kill-switch `BANXE_DSE_LIVE_ALLOWED=true` + `BANXE_DSE_MARKET_BASE_URL` (public Indexer). `BANXE_DSE_MARKET_API_KEY` is **kaiko-only — unused for dydx**. Mock-default, **fail-closed** to mock on kill-switch-off / provider error.
- **Provider:** dYdX v4 Indexer public REST + `v4_orderbook` WS → §D2 `{type,data:{bids,asks,sequence}}` unchanged; **API-only, no AGPL vendoring** (ADR-140/ODR-4, no §13); decimal strings (I-01, never float).
- **Advisory-moat:** read-only market data only — no orders/execution/custody/keys; feeds DSE advisory (advice→unsigned-intent→client-signs). Public data → non-custodial, no PII, no Travel-Rule, no MiCA execution surface.
- **Gate:** ✅ **EXECUTABLE** (Phase-1: no keys, no legal, no new ADR) — distinct from prod-live (`S6.x-PROD`, ODR-gated: keys + MiCA/CASP). Spec includes acceptance criteria + conformance-test plan (selection matrix, §D2 envelope, sequence/snapshot-on-gap, I-01, fail-closed) — note the deeper re-snapshot-on-reconnect hardening was former S6.7 (now DROPPED per ADR-094); minimal monotonic-sequence + snapshot-reseed is in-scope, deeper hardening is a separate non-blocking concern.
- **Proof:** docs-only (one spec in `banxe-architecture`); **no code / runtime / port / FROZEN-contract / new repo / keys / secrets / RAR content**; 0 files deleted (append-only I-24). IL **provisional, NOT hardcoded** (ADR-119 Rule 8) — `build_ledger.py` mints over current `origin/main` (rebased onto `5fb2b0c`, base frozen max 721) → **IL-722** via the ADR-143 central allocator (720 taken concurrently by #879 — collision resolved by max+1, no renumber per ADR-119 Rule 4). Re-rebased per Rule 2/5 after concurrent ledger PRs #879/#880 advanced main — a rebase signal, not a stop-barrier; `--force-with-lease` only. Append-only (ADR-059-A): ONE tail shard, il_ts `2026-06-29T05:00:00Z` strictly > origin/main max. Branch `agent/factory/docs/s62en-dydx-marketdata-spec` off origin/main `5fb2b0c` (ADR-120; namespace ADR-060).
- **Status:** DONE — spec authored. Authorises a later operator-gated `banxe-trading-backend` flag-enable build (own GO). **DRAFT PR; DO NOT MERGE — operator-gated (§71).**
- **Refs:** `docs/specs/S6.2-EN-DYDX-MARKETDATA-SANDBOX-LIVE-SPEC-2026-06-29.md`; `docs/roadmap/TRADING-BLOCK-ROADMAP-AND-SPRINTS-2026-06-28.md` (IL-714); ADR-083/021/102/140/094/119/143/059-A/120/060; ODR-4; IL-195; I-01; `dse-live-providers-options.md`; banxe-trading-backend `ports/{dydx_market_data,market_data_port}`, `tests/test_dydx_market_data`. Operator HITL.
