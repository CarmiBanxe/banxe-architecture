---
il_ts: 2026-07-02T06:00:00Z
session_id: agent-factory-governance-fleet-monitor-build-prompt
source: CEO
status: DONE
---
### [OWNER: A] Fleet heartbeat monitor + alert-sink BUILD-PROMPT — transferable infra task (placement audit-resolved)
- **Decision:** Per operator "audit resolved placement", authored `docs/governance/FLEET-MONITOR-BUILD-PROMPT.md` — the canonical build-prompt for constructing the fleet heartbeat monitor + alert-sink, derived from SERVER-CONTROL-ORCHESTRATION (#959/IL-807) + `config/fleet/*.yaml`. **This is a TASK, not code**: writes no monitor/daemon/enforcer/alert-sink, touches no `fabric/legion/*` runtime, machines, gateway, perimeter, or `config/fleet/*`. **PREPARE-ONLY**, Draft PR. Owner A.
- **Placement (audit-resolved, canon-grounded):** monitor + alert-sink → `fabric/legion/` (EXISTING Legion-side ops subtree — gate-exec.service et al.; always-on locus per #959 §4; reads gateway :4000 + host telemetry READ-ONLY; does NOT act against machines). Alert-channel → Telegram via Hermes (ADR-126: Tier-1 read-only alerting-first HITL, out of scope merge/deploy/payment/AML) and/or n8n (GAP-066, port 5680); no payment/AML/merge action. #939 enforcer (mutating admit/drain/kill vs gateway) → project/infra repo beyond ADR-117 — **concrete repo [BLOCKING: operator confirms]** (banxe-emi-stack vs dedicated), NOT invented. Clean split: read-only monitor here-side, mutating enforcer project-side.
- **What monitor must do (from #959):** heartbeat probe every heartbeat_interval_s; stale > freshness_window_s; missed_beats ⇒ SUSPECT; health-check-before-use; one-retry; UNHEALTHY ⇒ fail-closed + alert; planned-window INFO vs unexpected on-host CRITICAL (evo1); monitor-survivability (Legion, not single-pointed on falling node); reads thresholds from config/fleet/heartbeat-policy.yaml (config-as-data, NOT hardcoded); READ-ONLY (no admit/drain/kill/power-cycle — that = separate enforcer).
- **Alert format:** [SERVER-ALERT] severity/host/event/affected_workloads/probable_cause_class/last_seen/next_action → Telegram via Hermes/ADR-126; severity from heartbeat-policy severity_map; within Hermes read-only/alerting envelope.
- **Acceptance (simulation-first):** validation drill #959 §8.7 (declare evo1 shutdown-window → INFO; simulate unexpected outage → CRITICAL + placement gated + journal); no live power-cycle; config-sourced thresholds (flip value changes behaviour, no code edit). Journal: reuse existing prometheus/journal (outages/recoveries/health-fail/shutdown-window/borrow-reclaim with correlation_id/host/cause_class/timestamp).
- **Boundaries:** NO monitor/daemon/enforcer/alert-sink code; `fabric/legion/*` runtime NOT touched; machines/gateway/perimeter/config/fleet/* NOT touched; #959 policy NOT touched; enforcer repo [BLOCKING operator] NOT invented. Only this build-prompt + shard. 0 off-scope.
- **Anti-dup (ADR-102) pointer-first:** references #959/IL-807, config/fleet/*, ADR-117, ADR-126 (Hermes), ADR-154, server-2 #932/#939, GAP-066 (n8n), fabric/legion/ (existing) — restates none; no parallel policy, no code.
- **Scope/flow:** authored per #900 — doc + paired shard ATOMIC; NO hand-edit of generated ledger; NO hardcoded IL (build_ledger mints, ADR-119 Rule 8). Re-mint discipline if collision: reset onto origin/main + regenerate; recreate shard AFTER reset (L-05); duplicate IL = rebase signal (L-06).
- **Proof:** IL provisional (ADR-119 Rule 8) — max+1 over origin/main (max 811; #964 took IL-811 mid-session → re-mint, 811 retained) via allocator (ADR-143/143-A); unique, 0 dups; 1:1 (ADR-144). Append-only: ONE tail shard, il_ts `2026-07-02T06:00:00Z` > main max. Fresh worktree off origin/main (ADR-120/060). FROZEN/.canon untouched.
- **Status:** DONE — fleet-monitor build-prompt + shard. **DRAFT PR; DO NOT MERGE — operator HITL. Next: operator confirms the [BLOCKING] enforcer repo; infra builds monitor (fabric/legion) + alert-sink (Telegram/Hermes) per this prompt; then the #939 enforcer project-side.**
- **Refs:** `docs/governance/FLEET-MONITOR-BUILD-PROMPT.md`; SERVER-CONTROL-ORCHESTRATION.md (#959/IL-807); config/fleet/server-inventory.yaml + heartbeat-policy.yaml; ADR-126 (Hermes); ADR-117; ADR-154; SERVER-2-* (#932/#939); GAP-066; fabric/legion/; ADR-102/119; #900. Operator directive 2026-07-02 (fleet-monitor build-prompt, placement audit-resolved).
