---
il_ts: 2026-06-25T13:00:00Z
session_id: agent-factory-factory-factory-report-collector
source: CEO
status: DONE
---
### Factory report collector — make «ОТЧЁТ ФАБРИКИ» executable: scripts/factory-report.sh (read-only A–G evidence collector) + Makefile `report` targets
- **Decision:** Created `scripts/factory-report.sh` (bash, `set -euo pipefail`, READ-ONLY audit, no service mutation) — the executable collector for the «ОТЧЁТ ФАБРИКИ» prompt (`docs/governance/FACTORY-STATUS-REPORT-PROMPT.md`), implementing the A–G data-collection from REAL repo/host evidence. Three modes: default **Russian plain-academic** text report, `--json` (machine-readable), `--self-test` (hermetic, repo-only, no host/network, exits 0). Added Makefile targets `report` / `report-json` / `report-self-test` (existing targets untouched).
- **Sections implemented:** A (git HEAD/main, worktrees, `ledger/build_ledger.py --check`, open PRs via `gh`); B (iterate ALL `agents/passports/*.yaml` via python+PyYAML → agent_id, level, line_of_defence, department (vertical), allowed_skills, allowed_callers→allowed_callees (horizontal)); C (`scripts/train.sh verify` → skill-gap status per mandatory skill); D (swarm classification Ruflo/OpenClaw/MiroFish/MetaClaw/other DERIVED by transparent keyword rule over agent_id+capabilities+description — not invented; counts emitted); E (hardware: local `ollama`/`docker` query, evo1/evo2 ssh read-only on host, degrades gracefully; reported vs topology Legion 64GB factory / evo1+evo2 128GB project); F (model list + honest model-card gap flag → MODEL-RISK-MANAGEMENT.md, AWAITS OPERATOR); G (quality schedule: per-PR `.githooks/pre-commit` + traffic-light cron presence; UI/UX pipeline `design_pipeline_agent.yaml` presence).
- **Basis:** conformance gap audit 2026-06-25 — the «ОТЧЁТ ФАБРИКИ» prompt existed but had NO executable collector (report produced ad-hoc); this closes that gap. Built against `origin/main 87c8b8f`; 57 passports; `docs/SKILLS-MATRIX.md` (10 skills, 7 mandatory); `scripts/train.sh` (verify); swarm evidence `AGENTS.md`/`.claude/agents/`; topology per operator + ADR-117.
- **Proof — --self-test green:** all four modes exit 0 with empty stderr — `--self-test` (RU text), `--self-test --json`, live text, live `--json`. Live JSON valid: mode=live, agents=57, open_prs=1, ledger=OK. Self-test JSON valid: 57 agents. Section B columns correctly aligned (empty middle fields preserved via `\x1f` Unit-Separator — fixed the `IFS=$'\t' read` whitespace-collapse bug). Two `set -e`+pipefail traps fixed during build: (1) `grep`-exit-1 on 0 open PRs, (2) standalone `cond && action` returning non-zero — replaced with `${VAR:-default}` and `if/fi`. JSON built robustly via env-vars + QUOTED heredoc (no shell interpolation of live data). File 160 lines (≤300), functions ≤50 lines (CLAUDE.md). `bash -n` clean; semgrep pre-commit green.
- **Automation coverage (honest):** **A, B, C, D, G = FULLY automated** (repo-local evidence). **E, F = PARTIALLY automated** — require host-side `ollama`/`docker`/ssh evo1·evo2 access; degrade gracefully to `n/a`/`host audit needed` when unreachable (and `self-test:skip` in hermetic mode). Model cards (F) flagged as a real gap (none in repo) → AWAITS OPERATOR.
- **Append-only (ADR-059-A):** ONE new tail shard at il_ts `2026-06-25T13:00:00Z` (strictly > fresh tree max `2026-06-25T12:30:00Z`); INSTRUCTION-LEDGER.md + IL-SEQUENCE.json regenerated via `python3 ledger/build_ledger.py`, `--check` exit 0. Isolated worktree off `origin/main 7f2b478` (ADR-120); RULE 7 self-honored (only own worktree). Anti-dup (ADR-102): no prior `factory-report.sh`.
- **Status:** DONE — «ОТЧЁТ ФАБРИКИ» is now executable (`make report` / `--json` / `--self-test`). DO NOT MERGE pending operator review.
- **Recommended next (operator):** merge PR; run `make report` on the legion host to exercise E/F with live `ollama`/ssh; deliver per-model cards so section F stops flagging the gap; optionally wire `make report-json` output into the dashboard model.
- **Refs:** `scripts/factory-report.sh` (NEW), `Makefile` (report targets); `docs/governance/FACTORY-STATUS-REPORT-PROMPT.md` (spec A–G); `docs/SKILLS-MATRIX.md`, `scripts/train.sh` (skills/verify); `agents/passports/*.yaml` (57); `AGENTS.md`, `.claude/agents/` (swarm types); `ADR-117` (factory↔project compute), `MODEL-RISK-MANAGEMENT.md` (model cards); `scripts/traffic-light.sh` + `deploy/cron/traffic-light.crontab` (G schedule); ADR-059-A, ADR-120, ADR-121, I-28.
