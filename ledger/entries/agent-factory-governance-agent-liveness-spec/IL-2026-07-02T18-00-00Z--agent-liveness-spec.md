---
il_ts: 2026-07-02T18:00:00Z
session_id: agent-factory-governance-agent-liveness-spec
source: CEO
status: DONE
---
### [OWNER: A] Agent-Liveness Spec (Sprint C) — agent-scoped 7/24 liveness contract + build-prompt; closes GAP-2/F-LIVE measurability
- **Decision:** Per operator (start master-plan sprints; first = agent-liveness), authored `docs/governance/AGENT-LIVENESS-SPEC.md` (new) — a governance contract + build-prompt for measuring agent liveness 7/24, agent-scoped analogue of fleet-control #959 (node-scoped). Changed ONLY this doc + paired shard. NO agent activated; NO engine installed; NO passport/ADR/config/perimeter/project-code/legal/ss1 touched; auth NOT bypassed (no 401 break). **PREPARE-ONLY**, Draft PR. Owner A.
- **Problem (§1, from #974/#982):** node-heartbeat exists (#959), agent-liveness does not; evo1 MoA answers on-demand (confirmed running #982) but on-demand ≠ provably 7/24; passport `active` ≠ process; without a contract live/idle/dead indistinguishable.
- **Contract (§2):** per-agent liveness signal (agent_id, last_seen, status live|idle|dead|unknown, host, invocation_count, via daemon|on-demand-gateway). TWO models: on-demand agents live ⟺ gateway-responds + route-registered + recent successful invocation within freshness_window (NOT a persistent daemon → distinguishes on-demand-live from dead); daemon agents = classical heartbeat. Freshness = honesty boundary: signal older than window ⇒ unknown, NEVER live-by-inertia; dead requires a failed check, not mere silence.
- **Thresholds (§3):** config-as-data (agent-liveness-policy.yaml schema; monitor reads as data, not hardcoded) — heartbeat_interval_s / freshness_window_s / idle_alert_after_s / invocation_min_window / retry_grace_s ALL marked [RATIFY] (operator, per #964 discipline; not fabricated).
- **Integration (§4):** extends #959 node→agent scope; reads through existing gateways (litellm :4000, evo1 MoA :8080) READ-ONLY via legitimate access/emitted metrics, auth NOT bypassed (401 not broken; monitor credential = [BLOCKING: operator]); events → factory journal + existing Prometheus (#964, reuse).
- **Alert (§5):** agent expected-live but dead/stale ⇒ [AGENT-ALERT] (analogue of [SERVER-ALERT] #959) via Telegram/Hermes (ADR-126 read-only alerting-first). Severity mirrors #959.
- **Utilization (§6):** 24/7 "must not idle" made MEASURABLE not enforced — idle > idle_alert_after_s ⇒ [AGENT-IDLE] observability signal (idle ≠ dead; idle = available for work); acting = orchestrator/operator, not decided here.
- **Build-prompt half (§7):** runtime monitor built infra/project-side (banxe-monitoring, #964 placement locus; analogue of fleet-monitor #963 one level down) — TASK not code; reads thresholds as data, emits AGENT-ALERT/AGENT-IDLE to Prometheus+Telegram, uses legitimate access (no 401 break). [BLOCKING: operator] credential + threshold ratification.
- **Boundaries (§8):** spec+build-prompt here; runtime built project-side; passports NOT touched (liveness read, not written into passport in this PR); no activation/install; auth not bypassed; legal/ss1 excluded.
- **Anti-dup (ADR-102) pointer-first:** references #959 (fleet-control), #974 (liveness-gap), #982 (runtime-addendum §7), #963 (monitor-build-prompt), ADR-126 (Telegram alert), ADR-117 (perimeter), #975 (Sprint C), #964 (placement), CLAUDE.md §10 — restates none; agent-scoped extension of #959, no parallel node-policy, no code.
- **Scope/flow:** authored per #900 — doc + paired shard ATOMIC; NO hand-edit of generated ledger; NO hardcoded IL (build_ledger mints, ADR-119 Rule 8). Re-mint if collision: reset onto origin/main + regenerate; recreate shard AFTER reset (L-05); duplicate IL = rebase signal (L-06).
- **Proof:** IL provisional (ADR-119 Rule 8) — max+1 over origin/main via allocator (ADR-143/143-A); unique, 0 dups; 1:1 (ADR-144). Append-only: ONE tail shard, il_ts `2026-07-02T18:00:00Z` > main max. Fresh worktree off origin/main (ADR-120/060). FROZEN/.canon untouched.
- **Status:** DONE — agent-liveness contract + build-prompt + shard. **DRAFT PR; DO NOT MERGE — operator HITL. Next (AWAITS-OPERATOR): ratify §3 thresholds; provide monitor read-credential; infra builds monitor in banxe-monitoring per §7. Then Sprint A/B/D.**
- **Refs:** `docs/governance/AGENT-LIVENESS-SPEC.md` (new); #959 (SERVER-CONTROL-ORCHESTRATION + config/fleet); #974 (AGENT-LIVENESS-GAP); #982 (master-plan §7); #963 (FLEET-MONITOR-BUILD-PROMPT); #964 (placement ratify); ADR-126/117; #975 (roadmap Sprint C); #978 (master-plan); CLAUDE.md §10; ADR-102/119/143/144; #900. Operator directive 2026-07-02 (Sprint C agent-liveness; measure 7/24; no activate/install/auth-bypass; passports untouched).
