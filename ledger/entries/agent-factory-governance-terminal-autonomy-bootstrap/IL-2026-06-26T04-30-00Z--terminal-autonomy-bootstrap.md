---
il_ts: 2026-06-26T04:30:00Z
session_id: agent-factory-governance-terminal-autonomy-bootstrap
source: CEO
status: DONE
---
### Terminal Autonomy Bootstrap — persist the AUTONOMY MANDATE that fixes the ask-vs-act regression on terminal start/reset
- **Decision:** Created `docs/governance/TERMINAL-AUTONOMY-BOOTSTRAP.md` — a PERMANENT, copy-paste bootstrap operators load as the FIRST message in any new/reset Claude Code terminal (central/left/right) to re-establish autonomous, answer-yourself behaviour before any task. Persists in-repo the fix that was applied ad-hoc twice this session, so it survives session resets and is no longer re-typed.
- **Contents (§1 copy-paste block + supporting sections):** (a) the AUTONOMY MANDATE — factory staff answer/resolve every question & blocker themselves by best-solution; never pause to ask the operator; never request clarification; never hand a decision back mid-task; auto-fix wrong preconditions (wrong repo/path, stale base, branch-exists, ledger conflict, missing data); ONLY operator-reserved action = the final squash-merge; surface only final results; (b) binding CANON summary BY REFERENCE (audit-first; one-artifact-per-step; isolated worktree off origin/main ADR-120; ADR-121 RULE 7 no destructive ops on shared/foreign state; append-only ledger --check; ADR-060 4-segment branches; no bypass; ADR-102 no-dup; stop before merge); (c) language rule (operator-facing Russian plain-academic no-flattery; technical artifacts English); (d) per-terminal confirm phrases (Central/Left/Right) + STANDBY instruction; (e) §0 "why this exists" — recurring ask-vs-act regression, fix proven 2×, `defaultMode=acceptEdits` confirmed NOT the cause.
- **Per-terminal confirm phrases:** Central → "Central terminal autonomous mode ACTIVE — канон принят, жду задачу."; Left → "Left terminal autonomous mode ACTIVE — канон принят, жду задачу."; Right → "Right terminal autonomous mode ACTIVE — канон принят, жду задачу." Then STANDBY (no action until a task is given).
- **Basis:** recurring incident 2026-06-25 — on terminal start/reset, factory staff drop into default interactive mode and ASK the operator instead of resolving by best-solution; the AUTONOMY MANDATE first-message fix was applied successfully twice this session; `.claude/settings.json defaultMode=acceptEdits` verified correct and NOT the cause (cause = missing standing autonomy instruction at terminal start).
- **ADR-102 (reference, not duplicate):** the doc REFERENCES `docs/adr/ADR-120-session-worktree-isolation.md`, `docs/adr/ADR-121-parallel-session-destructive-action-protection.md`, `.claude/rules/parallel-session-isolation.md` (Rules 1-7), `.claude/rules/{approval,safety}-rules.md` (best-decision + the stop-barriers that are the ONLY exception to "never ask"), `docs/governance/FACTORY-STATUS-REPORT-PROMPT.md` (§71-§74 consolidated left-terminal canon), `AGENTS.md` (Best Single Artifact) — adding ONLY the standing autonomy bootstrap + confirm phrases; modifies no ADR/canon. All 4 cross-ref targets verified present.
- **Proof:** doc created (`docs/governance/TERMINAL-AUTONOMY-BOOTSTRAP.md`); anti-dup (ADR-102) — no prior `*BOOTSTRAP*` doc in docs/governance/. Invented nothing beyond the proven fix (the mandate text mirrors the bootstrap applied 2× this session). Append-only (ADR-059-A): ONE new tail shard at il_ts `2026-06-26T04:30:00Z` (strictly > fresh tree max `2026-06-26T04:00:00Z`); INSTRUCTION-LEDGER.md + IL-SEQUENCE.json regenerated via `python3 ledger/build_ledger.py`, `--check` exit 0. Isolated worktree off `origin/main 607864c` (ADR-120); RULE 7 self-honored (only own worktree). semgrep pre-commit green.
- **Status:** DONE — autonomy bootstrap persisted; operators paste §1 + the per-terminal confirm phrase into any new terminal to prevent the ask-vs-act regression. DO NOT MERGE pending operator review.
- **Recommended next (operator):** merge; adopt §1 as the standard first-message on every terminal start/reset (central/left/right); optionally reference TERMINAL-AUTONOMY-BOOTSTRAP.md from the terminal launcher / onboarding so the bootstrap is loaded automatically.
- **Refs:** `docs/governance/TERMINAL-AUTONOMY-BOOTSTRAP.md` (NEW); `docs/adr/ADR-120-session-worktree-isolation.md`, `docs/adr/ADR-121-parallel-session-destructive-action-protection.md`, `.claude/rules/parallel-session-isolation.md`, `.claude/rules/{approval,safety}-rules.md`, `docs/governance/FACTORY-STATUS-REPORT-PROMPT.md`, `AGENTS.md`; `.claude/settings.json` (defaultMode=acceptEdits, not the cause); ADR-059-A, ADR-102, I-28.
