---
il_ts: 2026-06-19T02:00:00Z
session_id: agent-factory-m1-capability-breakdown-m1-22
source: CEO
status: DONE
---
### M1.22 capability breakdown (flatten account capabilities, code substep, additive/read-only) — BANXE.RAR → EMI
- **Instrukciya:** M1.22 code-подшаг: public read-only capability breakdown — GET /api/v1/catalogue/capability-breakdown → CapabilityBreakdown (by_capability: CapabilityCount{capability,count} из flatten account_metadata().accounts[*].capabilities, + total_accounts + total_memberships + source); второй list-valued flattened breakdown (после M1.21 networks), integer meta, зеркалит M1.21. capabilities = descriptive functional-role метки; flatten-семантика (multi→N buckets, empty→none, total_memberships≠total_accounts). supported_assets intentionally deferred; Midaz LedgerPort/FeeEnginePort НЕ вызывать; balances/postings absent. Distinct path /catalogue/capability-breakdown. Server-only evo1 isolated worktree (Rule 1, branch verified перед commit); Factory-Only Execution ([CLAUDE CODE]); advisory/read-only; I-01 (integer meta counts); fail-closed. Spec capability = non-exhaustive string (урок M1.16). Append-only IL-shard (ADR-059-A), il_ts строго выше re-fetched max по timestamp; promotion-PR без merge (ADR-102).
- **Shagi:** banxe-trading-backend ветка agent/factory/m1/capability-breakdown-m1-22 от main 6b1f8c6 (isolated worktree /tmp/wt-m122c). meta/breakdown.py: добавлены CapabilityCount(capability, count) + CapabilityBreakdown(by_capability, total_accounts, total_memberships, source) + capability_breakdown() (flatten account_metadata().accounts[*].capabilities → per-capability counts; deterministic sorted order; total_accounts=len(accounts), total_memberships=sum(counts); empty-capabilities → no bucket; lazy SANDBOX_MOCK; reuse уже импортированного account_metadata). api/catalogue.py: новый GET /catalogue/capability-breakdown на catalogue_router (distinct path); существующие endpoints не тронуты. docs/specs/catalogue-api.yaml: additive /v1/catalogue/capability-breakdown path + CapabilityBreakdown/CapabilityCount schemas; CapabilityCount.capability = type:string non-exhaustive (canonical fiat/crypto/self_custodial/internal примеры в description, без hard enum — урок M1.16); flatten-семантика (total_accounts vs total_memberships) задокументирована. Reuse account_metadata — без второго registry/counter/account-map. supported_assets НЕ surfaced (0 refs в breakdown.py); Midaz LedgerPort/FeeEnginePort НЕ вызваны (0 refs). Тесты tests/test_capability_breakdown.py (+6): characterization (by_capability == flatten group-by, total_accounts==len, total_memberships==sum==sum(len(capabilities)), ЯВНЫЙ flatten-тест multi→N/empty→none/memberships≠accounts, deterministic order), contract (AccountAdvisoryMetadata(6)/AccountMetadataResponse(3) frozen + /accounts/metadata + /catalogue/* unchanged + supportedAssets/supported_assets не surfaced + balance/posting/amount absent), fail-closed (empty-capabilities → no bucket, нет фабрикованных ключей "/unknown/none"). Валидация (main venv via PYTHONPATH): ruff clean, mypy strict clean (85 files), pytest 483 passed (477+6); smoke GET /catalogue/capability-breakdown 200 {byCapability crypto:1/fiat:2/internal:1/self_custodial:1, totalAccounts 3, totalMemberships 5 (5≠3: multi-capability accounts — flatten подтверждён), source sandbox-mock}, /accounts/metadata 200 unchanged. Promotion-PR banxe-trading-backend #55 OPEN (no merge). IL-shard в banxe-architecture isolated worktree.
- **Proof:** Duplication Audit (ADR-102): account_metadata/AccountAdvisoryMetadata.capabilities (M1.7) reuse — per-capability counts flattened, no second registry/account-map; Midaz LedgerPort/FeeEnginePort НЕ вызваны (0 refs); supported_assets intentionally deferred (0 refs, не surfaced); meta/breakdown.py (M1.16–M1.21) reuse module same flatten pattern; CapabilityBreakdown/CapabilityCount = derived summary, НЕ data SoT; AccountAdvisoryMetadata/AccountMetadataResponse/breakdown DTO не мутированы (frozen). Verdict: zero data duplicates, derived flatten summary only, keep/extend, no merge/delete. build_ledger --check exit 0. Numeric (I-01): только integer meta counts (per-capability/total_accounts/total_memberships), НЕ balances/amounts/positions/ledger postings/prices/APY/computed fees; no money Decimal/BigNumber/float; no balance/posting surface, no fee computation. Activation Safety: read-only/advisory/mock-safe derived flatten summary; no live orders/execution/balances/positions/ledger postings/payments/wallet-custody/fund movement; NO live Midaz LedgerPort/FeeEnginePort calls; NO infra/secret/PII/ops metrics; no network/keys; fail-closed (counts из fail-closed account_metadata; empty-capabilities → no bucket; no fabricated key). Spec-fidelity: capability non-exhaustive string, без hard enum; flatten-семантика задокументирована. Out-of-scope: P0/regulated/live (orders/execution/balances/positions/ledger postings/wallet-custody/payments/live Midaz LedgerPort), APY/yield/rates re-surface, fee computation/billing, KYC/AML, cards, crypto-wallet ops, internal/ops/infra metrics/secrets/PII, supported_assets flatten (deferred), мутация frozen DTO, новый registry/counter source, user/DB migrations, live state machines, contract-breaking. Server-only в isolated worktree (Rule 1/6, Factory-Only Execution); fabric/prod/secrets/emi-stack/governance вне M1.22 не тронуты; чужие/parallel-session ветки/untracked не тронуты; branch protection не обойдён; merge не делал.
- **Refs:** banxe-trading-backend PR #55 (agent/factory/m1/capability-breakdown-m1-22); src/banxe_trading_backend/meta/breakdown.py (capability_breakdown), api/catalogue.py (GET /catalogue/capability-breakdown), docs/specs/catalogue-api.yaml, tests/test_capability_breakdown.py; reuse accounts/metadata.py account_metadata/AccountAdvisoryMetadata.capabilities (M1.7 IL-272), meta/breakdown.py M1.21 flatten pattern (IL-317); ADR-013 (Midaz LedgerPort live SoT, untouched), ADR-102, ADR-103, ADR-059-A, I-01; docs/migration/M1.22-capability-breakdown.md (plan, PR #560).
