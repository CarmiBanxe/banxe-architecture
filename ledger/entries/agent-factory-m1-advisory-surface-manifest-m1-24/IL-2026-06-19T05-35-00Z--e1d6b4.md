---
il_ts: 2026-06-19T05:35:00Z
session_id: agent-factory-m1-advisory-surface-manifest-m1-24
source: CEO
status: DONE
---
### M1.24 advisory-surface manifest (static config-as-data inventory, code substep, additive/read-only) — BANXE.RAR → EMI
- **Instrukciya:** M1.24 code-подшаг: public read-only advisory-surface manifest — GET /api/v1/catalogue/advisory-surface → AdvisorySurfaceManifest (families: AdvisorySurfaceFamily{family,endpoint_count}, + total_families + total_endpoints + reused __version__ + source); открывает meta/inventory класс после исчерпания breakdown grid. КРИТИЧЕСКИЙ fence: НЕ программно перечислять app.routes (утекло бы live/regulated + infra /internal/* /healthz); только статический config-as-data curated список advisory M-track семейств. reuse __version__ (no second version source), не дублировать catalogue_meta (manifest=surface families, meta=data counts). Server-only evo1 isolated worktree (Rule 1, branch verified перед commit); Factory-Only Execution; advisory/read-only; I-01 (integer meta counts; нет uptime/latency/infra-метрик); fail-closed; Midaz LedgerPort/FeeEnginePort не вызывать; frozen DTO не мутировать. Spec family = non-exhaustive string (урок M1.16). Append-only IL-shard (ADR-059-A), il_ts строго выше re-fetched max по timestamp (05:05→05:35 после повторных параллельных коллизий timestamp на main); rebase+regenerate при CONFLICTING; promotion-PR без merge (ADR-102).
- **Shagi:** banxe-trading-backend ветка agent/factory/m1/advisory-surface-manifest-m1-24 от main 404caab (isolated worktree /tmp/wt-m124c). Новый meta/manifest.py: _ADVISORY_FAMILIES (config-as-data map family→paths: earn 3, accounts 1, assets 2, symbols 6, catalogue 8) + AdvisorySurfaceFamily(family, endpoint_count) + AdvisorySurfaceManifest(families, total_families, total_endpoints, version, source) + advisory_surface_manifest() (deterministic sorted; total_families=len(families), total_endpoints=sum(endpoint_count); reuse banxe_trading_backend.__version__; lazy SANDBOX_MOCK). НЕ программное перечисление app.routes; по построению исключает infra (/internal/*, /healthz) и live/regulated/sandbox (orders/execution/fees/quant/market_making/marketplace/sandbox*/baas_dss). api/catalogue.py: новый GET /catalogue/advisory-surface на catalogue_router (distinct path); существующие endpoints не тронуты. docs/specs/catalogue-api.yaml: additive /v1/catalogue/advisory-surface path + AdvisorySurfaceManifest/AdvisorySurfaceFamily schemas; AdvisorySurfaceFamily.family = type:string non-exhaustive (canonical earn/catalogue примеры, без hard enum — урок M1.16); config-as-data происхождение + fence (no /internal, no live/regulated) задокументированы. Reuse __version__ — без второго version source; catalogue_meta (M1.14) не продублирован (manifest=surface families vs meta=data counts). Midaz LedgerPort/FeeEnginePort НЕ вызваны (0 refs; единственное упоминание app.routes — docstring fence-note, не вызов). Тесты tests/test_advisory_surface_manifest.py (+5): characterization (families == {earn,accounts,assets,symbols,catalogue}, total_families==len, total_endpoints==sum==sum(len paths), version==__version__, deterministic order), negative-fence (ни одного infra/live/regulated семейства/пути /internal//healthz/orders/execution/fees/quant/market_making/marketplace/sandbox/baas в config И в response; balance/uptime/latency absent), contract (CatalogueMeta(6: *_count+source+version) + /catalogue/* unchanged), fail-closed (counts>0). Валидация (main venv via PYTHONPATH): ruff clean, mypy strict clean (86 files), pytest 499 passed (494+5); smoke GET /catalogue/advisory-surface 200 {families accounts:1/assets:2/catalogue:8/earn:3/symbols:6, totalFamilies 5, totalEndpoints 20, version 0.1.0 (==__version__), source sandbox-mock; нет infra/live токенов}, /catalogue/meta 200 unchanged. Promotion-PR banxe-trading-backend #58 OPEN (no merge). IL-shard в banxe-architecture isolated worktree.
- **Proof:** Duplication Audit (ADR-102): __version__ (0.1.0) reuse — no second version source (как catalogue_meta); catalogue_meta (M1.14) distinct не дублирован (meta=catalogue DATA counts vs manifest=advisory SURFACE families, нет пересечения подсчитываемого); _ADVISORY_FAMILIES = config-as-data curated list (НЕ route scan, не второй route registry); Midaz LedgerPort/FeeEnginePort НЕ вызваны (0 refs); CatalogueMeta + M1.x frozen DTO не мутированы; AdvisorySurfaceManifest/AdvisorySurfaceFamily = derived/config summary, НЕ data SoT. Verdict: no second version/registry source, config-as-data only, keep/extend, no merge/delete. build_ledger --check exit 0. Numeric (I-01): только integer meta counts (endpoint_count/total_families/total_endpoints); НЕ balances/amounts/positions/ledger postings/prices/APY/computed fees/uptime/latency/request-rates/infra metrics; no money Decimal/BigNumber/float. Activation Safety: static config-as-data manifest, advisory/mock; no live orders/execution/balances/positions/ledger postings/payments/wallet-custody/fund movement; NO live Midaz LedgerPort/FeeEnginePort calls; NO infra/secret/PII/ops/observability metrics; NO uptime/health-of-services exposure; NO программное app.routes enumeration (fence против /internal/* /healthz live/sandbox leakage); fail-closed (fixed curated list; no non-advisory/infra endpoint; no fabricated family). Spec-fidelity: family non-exhaustive string, без hard enum; config-as-data/fence задокументированы. Grid-completion: breakdown grid исчерпан, M1.24 открывает meta/inventory класс. Out-of-scope: P0/regulated/live (orders/execution/balances/positions/ledger postings/wallet-custody/payments/live Midaz LedgerPort), APY/yield/rates re-surface, fee computation/billing, KYC/AML, cards, crypto-wallet ops, internal/ops/infra metrics/secrets/PII /internal/* /healthz uptime latency health-of-services, программное app.routes enumeration, listing live/regulated/sandbox/baas, второй version source, мутация catalogue_meta/frozen DTO, user/DB migrations, live state machines, contract-breaking. Server-only в isolated worktree (Rule 1/6, Factory-Only Execution); prod/fabric/secrets/emi-stack/governance вне M1.24 не тронуты; чужие/parallel-session ветки/untracked не тронуты; branch protection не обойдён; merge не делал.
- **Refs:** banxe-trading-backend PR #58 (agent/factory/m1/advisory-surface-manifest-m1-24); src/banxe_trading_backend/meta/manifest.py (advisory_surface_manifest), api/catalogue.py (GET /catalogue/advisory-surface), docs/specs/catalogue-api.yaml, tests/test_advisory_surface_manifest.py; reuse banxe_trading_backend/__init__.py __version__, distinct from meta/catalogue.py catalogue_meta (M1.14 IL-? / catalogue meta); ADR-102, ADR-103, ADR-059-A, I-01; docs/migration/M1.24-advisory-surface-manifest.md (plan, PR #570).
