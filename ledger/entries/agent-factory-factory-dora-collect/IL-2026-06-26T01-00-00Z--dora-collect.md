---
il_ts: 2026-06-26T01:00:00Z
session_id: agent-factory-factory-dora-collect
source: CEO
status: DONE
---
### DORA collector (repo-derived proxy) — scripts/dora-collect.sh: compute D-1..D-4 from git/PR data vs KPI-DORA targets; live infra AWAITS OPERATOR
- **Decision:** Created `scripts/dora-collect.sh` (bash, `set -euo pipefail`, READ-ONLY, no mutation) computing the four DORA keys from **repo-available evidence** (merged PRs to main via `gh`) over a window (default 30d, `--since N`), each compared to the `docs/governance/KPI-DORA-FRAMEWORK.md` target with a 🟢/🟡/🔴 verdict. Closes the **computable** part of the S3 DORA gap honestly — live Prometheus/Grafana(evo2) collection (KPI-DORA-FRAMEWORK §4.2) remains **AWAITS OPERATOR** and is explicitly NOT invented. Added Makefile targets `dora`/`dora-json`/`dora-self-test` (existing untouched) + a single non-breaking reference line in `scripts/factory-report.sh` section F.
- **Metrics + honest heuristics (all labelled "repo-derived proxy"):**
  - **D-1 Deployment Frequency** = merged-PR count in window / days (target ≥1/day). Verdict 🟢≥1 / 🟡≥0.5 / 🔴.
  - **D-2 Lead Time** = median(PR `createdAt`→`mergedAt`) in days (proxy: open-to-deploy; lightweight — avoids the multi-MB per-PR commits payload that overflows env limits). Target <1d. 🟢<1 / 🟡<2 / 🔴.
  - **D-3 Change Failure Rate** = % of merged PRs whose title matches `revert|rollback|hotfix` (documented heuristic). Target ≤15%. 🟢≤15 / 🟡≤25 / 🔴.
  - **D-4 MTTR (approx)** = median(createdAt→merged) of those failure-marker PRs, as an approximate restore time (flagged approximate). Target <1h. 🟢<1 / 🟡<4 / 🔴. ⚪ when no data.
- **Engineering decisions (best-solution, recorded):** (a) `now` is **data-anchored** = max(mergedAt) in live mode, not host `date -u` — the host wall-clock is skewed vs the repo's PR timestamps, which otherwise emptied the window; (b) dropped the `gh ...,commits` field (400-PR payload overflowed the env var → json.loads failed → empty) in favour of createdAt→merged lead-time; (c) `--self-test` and `--json` are **orthogonal flags** (data-source vs output-format) — fixed an earlier single-MODE bug where `--json` overrode `--self-test`; (d) honest **cap flag**: when the 400-PR fetch limit truncates the window, D-1 is labelled a lower bound (reduce `--since`).
- **Basis:** KPI-DORA-FRAMEWORK.md D-1..D-4 targets (RECONCILED 2026-06-22: ≥1/day, <1d, ≤15%, <1h) + §4.2 (live collection AWAITS OPERATOR) + the `factory-report.sh` section-F gap ("DORA not collected"); merged-PR data via `gh pr list`.
- **Proof — --self-test green + sample live reading:** `bash scripts/dora-collect.sh --self-test` (synthetic 5 PRs, 2 failure-markers) → text report, exit 0; `--self-test --json` → valid JSON (merges=5, capped=false), exit 0; `make dora-self-test` exit 0. Live (real merged PRs): 7d window → 252 merges, **D-1 36.0/day 🟢, D-2 0.01d 🟢, D-3 0.4% (1/252) 🟢, D-4 0.03h 🟢**; 30d window → 400 merges (capped, D-1 lower-bound noted). `factory-report.sh --self-test` still exit 0 with the new F reference line (non-breaking). `bash -n` clean; 113 lines (≤300); semgrep pre-commit green.
- **Append-only (ADR-059-A):** ONE new tail shard at il_ts `2026-06-26T01:00:00Z` (strictly > fresh tree max `2026-06-26T00:30:00Z`); INSTRUCTION-LEDGER.md + IL-SEQUENCE.json regenerated via `python3 ledger/build_ledger.py`, `--check` exit 0. Isolated worktree off `origin/main 0b249af` (ADR-120); RULE 7 self-honored. Anti-dup (ADR-102): no prior `scripts/dora-*`.
- **Status:** DONE — repo-derived DORA collector in place (`make dora`); the computable S3 part is honest and green. Live infra metrics remain operator-gated. DO NOT MERGE pending operator review.
- **Recommended next (operator):** merge; stand up the canonical Prometheus/Grafana DORA pipeline on evo2 (KPI-DORA-FRAMEWORK §4.2, AWAITS OPERATOR) for production telemetry; optionally schedule `make dora` alongside the traffic-light cron for a daily repo-derived snapshot.
- **Refs:** `scripts/dora-collect.sh` (NEW), `Makefile` (dora targets), `scripts/factory-report.sh` (§F reference line); `docs/governance/KPI-DORA-FRAMEWORK.md` (targets + §4.2 AWAITS OPERATOR); `scripts/traffic-light.sh` (cron precedent); ADR-059-A, ADR-120, ADR-121, I-28.
