---
il_ts: 2026-06-26T12:30:00Z
session_id: agent-factory-governance-adr-123-operator-apply-runbook
source: CEO
status: DONE
---
### ADR-123 apply-runbook — operator by-hand procedure for `~/.claude/settings.json` hardening (no config edit, no merge)
- **Decision:** Created `docs/runbooks/ADR-123-apply-global-claude-settings.md` — the step-by-step **operator runbook** for applying the ADR-123 `concept_only` fragment to `~/.claude/settings.json` by hand. The factory does NOT edit the home file (ADR-103, operator-local boundary) and does NOT merge ADR-123 (PR #787) — both are explicit operator steps. This shard adds only the runbook + ledger entry; it mutates no project behaviour and no global config.
- **Context (audit 2026-06-25, HEAD `ec95496`):** Verify showed the merge precondition for ADR-123 is **NOT satisfied** (fail-closed): global `~/.claude/settings.json` still has `skipDangerousModePermissionPrompt: true`, a blanket `Bash(git push:*)` allow, and an empty push-deny. The runbook is the missing operator procedure to close that gap before ADR-123 goes live.
- **Runbook content (4 parts):** (1) before→after + unified `diff` of the permission-relevant keys, with the exact target JSON — `allow` narrowed to feature-push (`git push`, `git push origin HEAD`, `git push -u origin HEAD`, `git push --dry-run *`, `git push origin *`); `deny[]` mirrors project (origin main/master, `* HEAD:main|master`, `--force`/`-f`, `git -C *` variants, `npm publish *`); `skipDangerousModePermissionPrompt` removed; `npm:*` → explicit subcommand allows. (2) VERIFY block: `jq` assertions on the file (skipDangerous absent, deny contains main/master + HEAD/-force/-C/npm-publish, blanket allows removed) **plus an explicit warning** that `git push --dry-run` in bash does NOT validate the Claude-permission layer (it exercises git's network path and bypasses `settings.json`) — permission enforcement is verified only via jq-on-file AND observed Claude Code tool-call behaviour, never the bare shell. (3) ADR-060 cross-check: confirm GitHub branch protection + Merge Queue are ON server-side (gh api + GraphQL probes), because `main` is protected server-side, NOT by the local pre-push hook (which only gates branch-naming) — else `main` safety would rest on the Claude config alone. (4) backup/rollback + done-criteria checklist.
- **Defense-in-depth framing:** ADR-123 (client/Claude permission layer) is additive ON TOP OF ADR-060 (server-side branch protection + Merge Queue), never a replacement. The runbook makes both layers explicit so neither is silently the sole defense.
- **No external config:** runbook authored from the live local audit + the canonical ADR-123 fragment in PR #787 only; NO settings.json or rules imported from the internet (task constraint). No git operations outside this branch; `~/.claude/settings.json` NOT touched (ADR-103).
- **Proof:** runbook created at `docs/runbooks/ADR-123-apply-global-claude-settings.md`; target JSON mirrors the ADR-123 fragment verbatim (verified against PR #787 diff). Append-only (ADR-059-A): ONE new tail shard at il_ts `2026-06-26T12:30:00Z` (strictly > fresh-tree max `2026-06-26T11:30:00Z`); INSTRUCTION-LEDGER.md + ledger/IL-SEQUENCE.json regenerated via `python3 ledger/build_ledger.py`, `--check` exit 0. IL number provisional (ADR-119 Rule 8) — frozen at merge time after rebase onto current origin/main (ADR-123/PR #787 IL-540 still unmerged; this shard takes the next free number deterministically). RULE 7 self-honored; no git ops outside this branch.
- **Status:** DONE — operator runbook recorded. `~/.claude/settings.json` NOT edited and ADR-123 (PR #787) NOT merged — both are operator steps, by design. DO NOT MERGE this PR pending operator review.
- **Recommended next (operator):** (1) apply the §1.2 fragment by hand to `~/.claude/settings.json` per the runbook; (2) run the §3 VERIFY block (jq + Claude Code behaviour, NOT bare `--dry-run`); (3) §4 confirm branch protection + Merge Queue ON; (4) then merge ADR-123 (PR #787) and this runbook PR.
- **Refs:** `docs/runbooks/ADR-123-apply-global-claude-settings.md` (NEW); `docs/adr/ADR-123-claude-permissions-hardening.md` (PR #787, IL-540 — the decision, concept_only); `.claude/settings.json` (project — unchanged), `~/.claude/settings.json` (global — AWAITS OPERATOR); ADR-027 (permissions reclassification), ADR-060 (branch-name gate + server-side branch protection / Merge Queue), ADR-103 (no operator-local mutation), ADR-120 (push branch, never merge), ADR-121 + `parallel-session-isolation.md` Rule 7/8, ADR-119 (stable IL numbering); CLAUDE.md §11/§12, `.claude/rules/safety-rules.md`, `.claude/rules/approval-rules.md`.
