---
il_ts: 2026-06-26T11:30:00Z
session_id: agent-factory-governance-adr-anthropic-finance-templates
source: CEO
status: DONE
---
### ADR-122 — Mapping Anthropic finance agent templates → BANXE factory (skills/connectors/subagents); NO fork, reference-only
- **Decision:** Created `docs/adr/ADR-122-anthropic-finance-templates-mapping.md` (ACCEPTED, concept_only) mapping the 10 Anthropic finance agent templates (2026-05-05; Pitch builder, Meeting preparer, Earnings reviewer, Model builder, Market researcher, Valuation reviewer, General ledger reconciler, Month-end closer, Statement auditor, KYC screener) onto the existing BANXE agent layer, and fixing the decision: **DO NOT fork `anthropics/financial-services`** — borrow ONLY the 3-layer reference pattern (skills + connectors + subagents) as a design checklist where a gap exists. No external code/plugin/template imported; gap-closure is future PROPOSED passports (I-27), not import.
- **3-layer pattern already native in BANXE:** skills = `docs/SKILLS-MATRIX.md` (10 skills bound 57/57); connectors = adapter passports (jube/watchman/yente, midaz_mcp, clickhouse_writer) + hexagonal `ports:`; subagents = orchestration tree (`.claude/rules/agents.md`: L1 orchestrator → L2 sub-agents) + `agents/souls/` (19) + `agents/swarms/` (3).
- **Mapping result (10 templates → BANXE agent/passport → gap):** PRESENT 3 — General ledger reconciler (`safeguarding_recon_governor` + `clickhouse_writer` + `midaz_mcp_agent`, CASS-15 recon), Statement auditor (`internal_audit_agent` + `safeguarding_audit_agent` + `reporting_agent`), KYC screener (full AML/KYC stack: `sanctions_check`/`watchman_adapter`/`yente_adapter`/`adverse_media_governor`/`aml_orchestrator`/`banxe_aml_orchestrator`/`tx_monitor`/`crypto_aml`/`jube_adapter`/`case_management_agent`). PARTIAL 5 — Earnings reviewer (`cfo_orchestration_agent`+`reporting_agent`), Model builder (`treasury_alm_agent`+`ml_pipeline_agent`), Market researcher (MiroFish research swarm + `experiment_copilot_agent`), Valuation reviewer (`risk_oversight_agent`), Month-end closer (`cfo_orchestration_agent`+`reporting_agent`+`safeguarding_recon_governor`). GAP 2 — Pitch builder + Meeting preparer (front-office analyst agents BANXE does not have; dept-head stubs only). The compliance core matches the strongest external templates; the front-office/analyst templates are the gaps.
- **No-fork rationale (ADR-102):** BANXE already implements the 3-layer pattern on its own governed canon; forking `anthropics/financial-services` would duplicate an externally-governed agent set, cross the ADR-117 perimeter, and bypass passport/skill governance (I-27/I-28). Repo has remote `origin` only — no fork/upstream.
- **AWAITS OPERATOR (open questions, not asserted):** connectors to **боевые/production data** (perimeter ADR-117; on-prem inference hard rule); **KYC/AML регламенты** — screening lists/EDD rules/risk thresholds/SAR workflow (operator/MLRO, MLR 2017/FCA); whether to build the 2 GAP + 5 PARTIAL front-office/analyst agents (operator/product decision); numeric thresholds/model classification for any new finance agent (operator/CRO, MRM §6).
- **Basis:** external fact verified by search — Anthropic 10 finance agent templates, 3-layer reference architecture, plugin + Managed-Agents cookbook (`github.com/anthropics/financial-services`, `anthropic.com/news/finance-agents`, 2026-05-05); live audit of the BANXE agent layer (57 passports, 19 souls, 3 swarms, SKILLS-MATRIX 10 skills, orchestration tree in `.claude/rules/agents.md`).
- **Proof:** ADR-122 created (next free ADR number; global max across docs/adr + decisions = 121 → 122); anti-dup (ADR-102) — no prior anthropic/finance/financial-services ADR. No fork performed; no external code imported; concept_only ADR. INSTRUCTION-LEDGER linked via il_anchor IL-539. Append-only (ADR-059-A): ONE new tail shard at il_ts `2026-06-26T11:30:00Z` (strictly > fresh tree max `2026-06-26T11:00:00Z`); INSTRUCTION-LEDGER.md + IL-SEQUENCE.json regenerated via `python3 ledger/build_ledger.py`, `--check` exit 0. Isolated worktree off `origin/main fb5d79d` (ADR-120); RULE 7 self-honored; no git ops outside this branch.
- **Status:** DONE — ADR-122 ACCEPTED (governance/concept-only); decision recorded (no fork, reference-pattern-only), 10-template mapping + gaps + AWAITS-OPERATOR open items. DO NOT MERGE pending operator review.
- **Recommended next (operator):** decide which GAP/PARTIAL front-office analyst agents (if any) to author as PROPOSED passports using the 3-layer checklist; resolve the AWAITS-OPERATOR items (production-data connectors, KYC/AML regulations) before any KYC-screener mapping is activated.
- **Refs:** `docs/adr/ADR-122-anthropic-finance-templates-mapping.md` (NEW); `docs/SKILLS-MATRIX.md`, `docs/SKILLS-OPERATING-MODEL.md`, `.claude/rules/agents.md`, `banxe-subagent-context.md`, `agents/passports/` (57), `agents/souls/` (19), `agents/swarms/` (3); `github.com/anthropics/financial-services` (external reference); ADR-102 (no-dup), ADR-117 (perimeter), ADR-059-A, ADR-120, ADR-121, I-27/I-28.
