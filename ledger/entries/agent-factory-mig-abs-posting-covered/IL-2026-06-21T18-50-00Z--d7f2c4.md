---
il_ts: 2026-06-21T18:50:00Z
session_id: agent-factory-mig-abs-posting-covered
source: CEO
status: DONE
---
### MIG-ABS-posting — COVERED by existing GL/posting subsystem (resolves blocker IL-404/PR #648) — operator decision A, no scaffold
- **Instrukciya:** Operator решение A по MIG-ABS-posting blocker (double-entry GL posting via LedgerPort уже в emi-stack): declare-covered — mounted GLService/posting subsystem (IL-FIN-01) удовлетворяет abs-posting delta. Только фабрика; advisory-only; isolated worktree (Rule 1/6); docs-only PR без merge; IL append-only (ADR-059-A), il_ts строго выше re-fetched max; no scaffold/code.
- **Decision:** ABS delta abs-posting = CLOSED done-by-existing (covered services/ledger/ GL/posting subsystem); no new posting port / no second ledger; GLService (IL-FIN-01) = canonical posting seam via LedgerPort; Midaz = single ledger SoT (ADR-013); legacy abs-posting.service = RETIRE after (covered, merge-then-retire M1.2 ABS lineage).
- **Covering surface (на main, IL-FIN-01):** double-entry models = services/ledger/ledger_models.py (Posting Decimal I-01, JournalEntry sum debits==sum credits per currency, PostingDirection/Status); GL posting via LedgerPort = gl_service.py GLService.post_journal_entry() consume LedgerPort + UnbalancedEntryError + high-value approval; payment→posting mapping = payment_posting_service.py (PaymentEvent→PostingRule) + posting_rules.py (PostingRuleEngine); ledger adapter single SoT = midaz_adapter.py (Midaz ADR-013) + inmemory_ledger.py. Consumers: api/routers/ledger.py, api/deps.py, services/recon/midaz_reconciliation.py. Double-entry+balance-invariant+LedgerPort+Midaz machinery уже реализована и consumed — parallel ABS posting port = дубликат (ADR-102).
- **Residual (deferred, optional, НЕ часть covered):** единственный candidate ABS-specific slice = ABS PostingRule (legacy ABS payment-event → GL debit/credit account types) добавленный в существующий PostingRuleEngine и posted via GLService = rule/config addition, НЕ новый порт / второй ledger, НЕ live posting; deferred optional только если distinct ABS event→GL mapping product-required (tracked под GL posting-rule family, advisory, operator-gated для live activation).
- **Proof:** Backlog update: abs-posting → already in emi-stack (GLService/posting subsystem IL-FIN-01), removed из ABS-delta scaffold backlog; remaining ABS delta = scoring/agreement/customer-contract/credential/legal-entity/info-field/process+cron port (preflight-first, may also be covered) + abs-customer re-home → identity (consume existing customers surface); sibling backlog unchanged (M2.4c-e OB delta, M2.3 auth delta, KYC/KYB/AML pending I-27, M2.8 frontend after roster audit). Что сделано (read-only): mandatory preflight + ADR-102 audit (blocker #648); covered-note + IL-shard. НЕ сделано: никакого scaffold (ни в одном репо); никакого AbsPostingPort/posting DTO; никакого backend PR; banxe-emi-stack/ledger.py/Midaz не тронуты; KYC carve-out не тронут; merge не делал. Advisory-only covered-note; shell read-only; append-only IL (il_ts 18:25 > re-fetched max 2026-06-21T17:57:23Z и выше собственного blocker 18:10); build_ledger --check exit 0; docs-only PR; без merge.
- **Status:** MIG-ABS-posting CLOSED (done-by-existing; no scaffold); blocker PR #648 resolved (operator A). GLService = canonical posting seam; Midaz single SoT; legacy abs-posting retire-after. KYC/KYB/AML — только после I-27 sign-off.
- **Recommended next:** следующий ABS delta — recommend abs-customer re-home → identity (consume существующий mounted customers.py/customer_lifecycle.py identity-core surface; mandatory preflight first — likely covered/reconcile) ИЛИ ABS sub-service port (scoring/agreement/contract — preflight-first). KYC/KYB/AML — только после I-27 sign-off; M2.8 frontend — после roster audit.
- **Refs:** docs/migration/MIG-ABS-posting-COVERED-gl-service.md; MIG-ABS-posting-BLOCKER (IL-404, PR #648); read-only origin/main banxe-emi-stack services/ledger/{ledger_models,gl_service,posting_rules,posting_models,payment_posting_service,midaz_adapter,ledger_port}.py + api/models/ledger.py + api/routers/ledger.py; MIG-M2.5 (ABS reconcile — abs-posting re-home target), MIG-M2.4a (declare-covered precedent), MIG-M2.4-INT (thin-integration precedent); ADR-013, ADR-102, ADR-103, ADR-059-A, I-01, I-24, I-28; /tmp/banxe-migration-mapping-v0.claude.txt.
