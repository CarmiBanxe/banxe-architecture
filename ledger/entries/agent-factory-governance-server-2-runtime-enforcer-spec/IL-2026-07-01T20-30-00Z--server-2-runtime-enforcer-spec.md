---
il_ts: 2026-07-01T20:30:00Z
session_id: agent-factory-governance-server-2-runtime-enforcer-spec
source: CEO
status: DONE
---
### [OWNER: A] Server-2 runtime-enforcer spec — implementation build-prompt (admission-controller for the ratified policy)
- **Decision:** Per operator "scope the runtime-enforcer", authored `docs/governance/SERVER-2-RUNTIME-ENFORCER-SPEC.md` — an IMPLEMENTATION-READY SPECIFICATION (build-prompt for the project/infra side), NOT the enforcer itself. Specifies exactly what the admission-controller/scheduler must do to physically execute admit/drain/deny/kill on the LiteLLM gateway (:4000) per the ratified policy (#932) and thresholds (#933/IL-780). **PREPARE-ONLY**, Draft PR. Owner A.
- **Behaviour (normative, executes policy):** admit (all borrowing conditions: primary queue idle ≥ idle_grace_s + health ok + concurrency < ceiling + budget/duty not exhausted); deny (any condition fails → refuse+log+project-only); drain (project_* arrival → stop admitting + graceful-drain to drain_deadline_s → complete or defer/cancel); kill (runaway > runaway_kill_after_s → hard-stop); project-priority invariant (project_* > factory_*, enforcer MUST NOT violate); fail-closed (telemetry down / health-fail / config unreadable → borrowing off, project-only).
- **Interfaces (contract, not implementation):** (a) gateway — tenant/class from alias prefix (project-*/factory-*), observe 235b single-slot busy/queue/in-flight, admit/deny/drain/kill via required integration point (concrete mechanism = project/infra decision, NOT prescribed); (b) factory journal — events borrow_grant/deny, revocation, preempt_defer, runaway_kill, budget_exhausted, health_fail with correlation_id/tenant/class/alias/duration/tokens/reason; (c) telemetry — REUSE existing LiteLLM prometheus callback + borrow counters (pointer, not reinvented).
- **Health-check-before-borrow (mandatory):** reachability + model-loaded + memory-headroom + error-rate-under-threshold; fail → deny+log health_fail+project-only.
- **Config binding:** enforcer READS `config/compute/server-2-borrow-policy.yaml` as read-only threshold source (Config-over-Hardcoding); MUST NOT hardcode/override/mutate; threshold change = config edit + re-ratification, never code.
- **Acceptance/simulation (testable, no hardware mutation):** dry-run job-trace proves project_critical before pending borrow within drain_deadline_s; borrow denied on non-empty queue/health-fail; factory duty-cycle ≤ max_duty_cycle + budget_exhausted fires; fail-closed under degraded telemetry/unreadable config; journal completeness; flipping a config value changes behaviour with NO code edit (proves no hardcoding). No live 235b load / no gateway change needed.
- **project_critical SLO:** still [RATIFY] (project-owned, #933) — enforcer accepts it as a config parameter; absent ⇒ uncapped-within-HITL placeholder; MUST NOT fabricate an SLO.
- **[НЕИЗВЕСТНО] (implementer/operator-sourced, not invented):** exact gateway-integration mechanism (LiteLLM-version-dependent — project/infra decision); project_critical SLO; enforcer deployment point (gateway/machines operator-owned, ADR-117).
- **Explicitly stated in-doc:** this is a specification-prompt; the actual enforcer is BUILT by the project/infra side under the operator gate (ADR-117 perimeter; gateway/machines operator-owned).
- **Boundaries:** NO enforcer/scheduler code; LiteLLM gateway config NOT touched; hardware/machines NOT touched; banxe-ui NOT touched; server-2-borrow-policy.yaml NOT touched (read-only consumer described); policy doc NOT touched. Only this spec + shard. 0 off-scope.
- **Anti-dup (ADR-102) pointer-first:** references SERVER-2-BORROWABLE-COMPUTE-ORCHESTRATION.md (#932), server-2-borrow-policy.yaml (#933), ADR-154, TERMINAL-OWNERSHIP, CONFLICT-LEDGER, existing prometheus callback — restates none; no parallel policy/config, no new agent.
- **Scope/flow:** authored per #900 — doc + paired shard ATOMIC; NO hand-edit of generated ledger; NO hardcoded IL (build_ledger mints, ADR-119 Rule 8).
- **Proof:** IL provisional (ADR-119 Rule 8) — max+1 over origin/main (max 781; #931/IL-781 landed mid-session → re-mint, 781 retained) via allocator (ADR-143/143-A); unique, 0 dups; 1:1 (ADR-144). Append-only: ONE tail shard, il_ts `2026-07-01T20:30:00Z` > main max. Fresh worktree off origin/main (ADR-120/060). FROZEN/.canon untouched. (Re-mint discipline if mid-session collision: reset onto origin/main + regenerate; recreate shard AFTER reset — staged-new does not survive reset --hard, lesson #933.)
- **Status:** DONE — enforcer build-prompt + shard. **DRAFT PR; DO NOT MERGE — operator HITL. Next: project/infra side builds the enforcer under operator gate per this spec; project_critical SLO when available.**
- **Refs:** `docs/governance/SERVER-2-RUNTIME-ENFORCER-SPEC.md`; SERVER-2-BORROWABLE-COMPUTE-ORCHESTRATION.md (#932); server-2-borrow-policy.yaml (#933/IL-780); ADR-102/117/154; TERMINAL-OWNERSHIP; CONFLICT-LEDGER; CLAUDE.md §10/§11; #900. Operator directive 2026-07-01 (scope the runtime-enforcer).
