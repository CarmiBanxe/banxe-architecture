# Cycle 011 Scope

## Scope of cycle-011-constitutional-materialization

This cycle covers the following scope:

1. Root CLAUDE.md — navigation entrypoint for AI assistants (existing file preserved; new constitutional version deferred to subsequent review).
2. Master prompts — DEVELOPERBLOCK.md and PROJECTEMI.md placed in constitution/ as primary working versions.
3. Amendment files — pending amendments placed in constitution/amendments/ for integration into master files.
4. INSTRUCTION-LEDGER.md — updated with cycle-011 execution record.
5. outcomes.md — cycle closure document.

## Constraints

This cycle scope is limited to the items listed above.

- No str_replace operations.
- No git tag operations.
- No atomic multi-file commits (GitHub web UI limitation).
- Claude-Code-required operations are deferred.
- Claude Code operations (atomic commits, tags, str_replace) are out of scope for this cycle.
- Each directive produces a single GitHub web UI commit.
- Explicit skip when a target file already exists.
- Partial success is acceptable; each directive reports independently.

## Execution Method

GitHub web UI via Perplexity Assistant.
