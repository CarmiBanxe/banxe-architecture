agent_id: internal_audit_agent
name: "InternalAuditAgent"
version: "0.1.0"
status: active
description: >
  STUB passport (Sprint-2 Staff Matrix). Mandatory **department-head agent** for the
  "Internal Audit (independent)" line of the canonical org chart (governance/CANONICAL-ORG-CHART-v2.md §9).
  PROPOSES only (I-27); NOT activated. No service code exists yet — service implementation is
  deferred to Sprint 3 (see GAP-078). Capabilities are intentionally a stub here.
level: 1
trust_zone: RED
bounded_context: CTX-01
autonomy: L2_REVIEW
capabilities:
  - department_head_orchestration  # TODO Sprint-3: specify code-derived capabilities
department: "Internal Audit (independent)"
human_double: "Internal Audit (Grant Thornton UK, outsourced)"
line_of_defence: "3rd Line — independent assurance, reports to Audit Committee / Board"
smf_function: "SMF5"
non_goals:
  - "Do NOT activate — PROPOSED stub only (I-27), gated on operator approval"
  - "Do NOT implement service code here — deferred to Sprint 3 (GAP-078)"
governance:
  change_class: CLASS_B
  owner: "Internal Audit (Grant Thornton UK, outsourced)"
  review_cycle: annually
  last_reviewed: "2026-06-21"
  approvers:
    - CEO
fca_references:
  - "SM&CR — Senior Management & Certification Regime (SMF5)"
parent_canon: "governance/CANONICAL-ORG-CHART-v2.md"
# ── Passport-bound skills (S-FAC-64; binding metadata only — does NOT activate, I-27) ──
allowed_skills:
  - context_memory_sync          # MANDATORY — IL/audit-trail continuity across assurance sessions (I-28)
  - rapid_spec_builder           # MANDATORY — 3rd-line dept-head PROPOSES IL/ADR for audit findings (I-27, I-28)
  - clean_architecture_enforcer  # MANDATORY advisory — enforce 3rd-line independence / no cross-contour imports (I-20)
prohibited_skills:
  - auto_refactor_pro            # PROHIBITED — independent assurance must not auto-refactor reviewed code (independence)
preferred_skill_sequences:
  scenario_governance:           # audit finding → IL/ADR proposal
    - context_memory_sync
    - rapid_spec_builder         # IL required before any audit recommendation lands (I-28)
    - clean_architecture_enforcer
    # quality-gate.sh MUST follow
mandatory_skill_triggers:
  context_memory_sync: ["any internal_audit_agent session start", "after any IL state change"]
  rapid_spec_builder: ["new audit finding", "new assurance recommendation", "ADR needed"]
# NOTE: error_handling_standardizer / performance_scanner / api_contract_guardian / smart_test_generator
# are NOT bound here — this is a stub with no service code or ports yet; deferred to Sprint-3 (GAP-078).
