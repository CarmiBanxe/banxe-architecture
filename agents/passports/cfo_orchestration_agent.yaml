agent_id: cfo_orchestration_agent
name: "CFOOrchestrationAgent"
version: "0.1.0"
status: active
description: >
  STUB passport (Sprint-2 Staff Matrix). Mandatory **department-head agent** for the
  "CFO Office" line of the canonical org chart (governance/CANONICAL-ORG-CHART-v2.md §9).
  PROPOSES only (I-27); NOT activated. No service code exists yet — service implementation is
  deferred to Sprint 3 (see GAP-078). Capabilities are intentionally a stub here.
level: 2
trust_zone: AMBER
bounded_context: CTX-10-REPORTING
autonomy: L2_REVIEW
capabilities:
  - department_head_orchestration  # TODO Sprint-3: specify code-derived capabilities
department: "CFO Office"
human_double: "CFO (David Goldstein)"
line_of_defence: "1st/2nd Line — finance"
smf_function: "SMF2"
non_goals:
  - "Do NOT activate — PROPOSED stub only (I-27), gated on operator approval"
  - "Do NOT implement service code here — deferred to Sprint 3 (GAP-078)"
governance:
  change_class: CLASS_B
  owner: "CFO (David Goldstein)"
  review_cycle: annually
  last_reviewed: "2026-06-21"
  approvers:
    - CEO
fca_references:
  - "SM&CR — Senior Management & Certification Regime (SMF2)"
parent_canon: "governance/CANONICAL-ORG-CHART-v2.md"

# ── Passport-bound skills (S-FAC-64; binding metadata only — does NOT activate, I-27) ──
allowed_skills:
  - context_memory_sync          # MANDATORY — IL/audit-trail continuity across dept-head sessions (I-28)
  - rapid_spec_builder           # MANDATORY — dept-head PROPOSES IL/ADR (I-27, I-28)
preferred_skill_sequences:
  scenario_governance:           # governance proposal → IL/ADR
    - context_memory_sync
    - rapid_spec_builder
    # quality-gate.sh MUST follow
mandatory_skill_triggers:
  context_memory_sync: ["any cfo_orchestration_agent session start", "after any IL state change"]
  rapid_spec_builder: ["new IL proposal", "new ADR needed", "finance governance recommendation"]
